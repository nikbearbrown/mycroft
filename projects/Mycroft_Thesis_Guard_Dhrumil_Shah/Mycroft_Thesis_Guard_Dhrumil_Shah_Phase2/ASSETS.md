# Assets and provenance

Everything on screen was either produced by your notebook or generated by the video code in this folder. No stock footage, no third-party imagery, no external data.

## Notebook figures (used as-is)

Written by Phase 2 Cell 15 and stored in `assets/mycroft_phase2_outputs/figures/`.

| File | Appears in | Shown as |
|---|---|---|
| `01_holdout_leaderboard.png` | B10 | Full figure after the animated leaderboard |
| `02_walk_forward_stability.png` | B08 | Full figure below the ranking bars |
| `03_roc_pr_calibration.png` | B11 | Whole figure, then its ROC, precision-recall and calibration panels |
| `04_regime_timeline.png` | B12 | Full figure with a slow zoom |
| `05_performance_by_regime.png` | B12 | Full figure after the animated bars |
| `06_new_feature_signal.png` | B13 | Full figure |
| `07_probability_distributions.png` | B13 | Full figure |
| `08_model_disagreement.png` | B13 | Split into two stacked panels for portrait |

Source: `mycroft_phase2_outputs/figures/` in the Colab run, copied from your Downloads folder. Matplotlib and seaborn output at 160 dpi.

## Generated for the video

| Asset | Made by | Used in |
|---|---|---|
| `architecture/mycroft_phase2_architecture.png` | `draw_architecture()` (Cell 6) | B15 |
| `charts/holdout_leaderboard_anim_final.png` | `hbar_spr()` (Cell 7) | B10 |
| `charts/purged_walk_forward_timeline.png` | `timeline_spr()` (Cell 7) | B06 |
| `charts/walk_forward_folds.png` | `fold_line_spr()` (Cell 7) | Only when the per-fold CSV is present |
| `code_screenshots/*.png` (5) | `code_spr()` with Pygments | B04, B05, B06, B07, B09 |
| `agent_visualizations/phase1_agent_chain.png` | `agents_spr()` (Cell 10) | B03 |
| `scenes/still_*.png` (16) | `render_frame()` | QA, thumbnails |
| `outputs/contact_sheet.jpg` | `contact_sheet()` | QA |

## Audio

| Asset | Source | Licence / terms |
|---|---|---|
| `narration/scene_01…16.mp3` | edge-tts, voice `en-US-AndrewNeural`, rate +27 % | Microsoft Azure neural TTS through the `edge-tts` client |
| `narration/narration_full.wav` | The 16 clips assembled on the beat timeline, peak-normalised to −1 dBFS | Derived from the above |
| Background music | None included | Add your own licensed track at `assets/background_music.mp3` |

If you prefer your own voice, record 16 files as `narration/scene_01_user.wav` … `scene_16_user.wav`. The generator prefers them over the synthetic clips, and beat lengths adjust to your recordings.

## Fonts

DejaVu Sans and DejaVu Sans Mono, bundled with matplotlib. The DejaVu fonts are distributed under a permissive licence that allows redistribution and embedding.

## Code shown on screen

Five panels quote your own notebook. Lines were re-wrapped to fit portrait width; no identifier, argument or value was changed. Sources: Cell 4 (`engineer_phase2_features`), Cell 5 (`MarketRegimeDetector.fit`), Cell 6 (`walk_forward_folds`), Cell 7 (`build_phase2_models`), Cell 10 (`build_ensembles`).

## Numbers

All metrics come from the printed output of Phase 2 Cells 3–14, mirrored in `data/phase2_results.json`. Where the metric files themselves are available (a Colab run), the generator reads them instead and re-checks every spoken figure.

## Third-party software

PyAV (FFmpeg bindings), Pillow, NumPy, pandas, matplotlib (fonts only), Pygments, edge-tts. All are open-source packages installed from PyPI; none of their assets appear in the video beyond the fonts noted above.
