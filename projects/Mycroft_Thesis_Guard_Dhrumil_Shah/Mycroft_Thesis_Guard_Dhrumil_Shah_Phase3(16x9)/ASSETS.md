# Visual asset list — Phase 3 (16:9)

Everything on screen came from your notebook or was generated by the code in this folder. No stock footage, no external data, no generated imagery.

## Notebook figures used (real output, unmodified)

Stored in `assets/phase3_figures/`, copied from the portrait project.

| File | Beat | Notebook source | Shown as |
|---|---|---|---|
| `agent1_01_market_cohesion_timeline.png` | B04 | Chart 1 | Left column, slow zoom; red bands mark the 12 stress windows |
| `agent1_02_sector_internal_correlation.png` | B04 | Chart 2 | Crossfade at 62 % of the beat |
| `agent2_01_event_timeline.png` | B05 | Chart 3 | Right column, captioned with the event count and threshold |
| `agent5_01_reliability_forest.png` | B08 | Chart 9 | Left column with the 0.50 reference line |
| `agent5_02_sector_regime_heatmap.png` | B08 | Chart 10 | Crossfade at 55 %; grey cells are suppressed small segments |

## Notebook figures not available

Not exported with the others, so beats B06 and B07 use real thresholds, formulas and a labelled schematic instead: `agent2_02_attribution_by_sector.png`, `agent3_01_cohort_position_map.png`, `agent3_02_cohort_dispersion.png`, `agent4_01_tail_risk_map.png`, `agent4_02_drawdown_recovery.png`.

Add them to `assets/phase3_figures/`, register them in the `FIG` dictionary in `phase3_video.py`, and re-render both cuts to include them.

## Generated for this cut

| Asset | Made by | Beat |
|---|---|---|
| Hub with the five agents in a row | `agent_row()` | B01 |
| Framework and data-layer chains | `flow_chain()` | B02, B03 |
| Stat cards (0.148 · 19.7 % · normal · 12) | `stat_card()` | B04 |
| Event-type bars (3,972 / 3,397 / 2,267) | `hbar_spr()` | B05 |
| Cohort chain and decoupling schematic | `flow_chain()`, `peer_schematic()` | B06 |
| Risk chain and tier threshold table | `flow_chain()`, `tier_table()` | B07 |
| Reliability label chips | `chip()` | B08 |
| Three-stage diagram, "Degrade, don't stop" card | `stage_diagram()`, `text_card()` | B09 |
| Full-width review queue table, field chips, governance cards | `queue_table()`, `text_card()` | B10 |
| Validation checklist and 33/33 score card | `checklist()`, `stat_card()` | B11 |
| Code panels (9) at 1680 px column width | `code_spr()` → `code_screenshots/*.png` | B02–B11 |
| Per-beat stills (11), contact sheet | `render_frame()`, `contact_sheet()` | QA |

All generated assets are re-rendered at landscape dimensions, so they differ from the portrait cut's versions even where the content is the same. The B06 peer-cluster graphic uses a fixed random seed and carries the on-screen caption "schematic - illustrates the method, not notebook data".

## Audio

| Asset | Source |
|---|---|
| `narration/scene_01…11.mp3` | edge-tts `en-US-AndrewNeural` at +25 % — the same files as the 9:16 cut |
| `narration/narration_full.wav` | The 11 clips on the beat timeline, peak-normalised to −1 dBFS |
| Background music | Not included; add `assets/background_music.mp3` and re-render |

Sharing the voiceover files is what keeps both cuts frame-accurate with each other.

## Fonts and software

DejaVu Sans and DejaVu Sans Mono (bundled with matplotlib, permissive licence). PyAV, Pillow, NumPy, Pygments, edge-tts — open-source packages from PyPI.

## Numbers

Every figure on screen traces to printed notebook output; see `FACTCHECK.md` and `data/phase3_results.json`.
