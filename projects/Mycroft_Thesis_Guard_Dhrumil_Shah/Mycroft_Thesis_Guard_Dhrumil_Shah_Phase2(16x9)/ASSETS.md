# Assets and provenance — 16:9 cut

Everything on screen came from your notebook or was generated by the code in this folder. No stock footage, no third-party imagery, no external data.

## Notebook figures (used as-is)

Written by Phase 2 Cell 15, stored in `assets/mycroft_phase2_outputs/figures/`.

| File | Appears in | Shown as |
|---|---|---|
| `01_holdout_leaderboard.png` | B10 | Right column, fading in at 60 % |
| `02_walk_forward_stability.png` | B08 | Right column with a slow zoom |
| `03_roc_pr_calibration.png` | B11 | Whole, then its ROC, PR and calibration panels |
| `04_regime_timeline.png` | B12 | Left column with a slow zoom |
| `05_performance_by_regime.png` | B12 | Right column after the animated bars |
| `06_new_feature_signal.png` | B13 | Centred, full content width |
| `07_probability_distributions.png` | B13 | Centred, full content width |
| `08_model_disagreement.png` | B13 | Centred and whole — its 2.8:1 shape suits landscape |

Source: the same eight PNGs as the portrait cut, copied from `Mycroft_Dhrumil_2_Project(9x16)`. Matplotlib and seaborn output at 160 dpi.

## Generated for this cut

| Asset | Made by | Used in |
|---|---|---|
| `architecture/mycroft_phase2_architecture.png` (and `_16x9.png`) | `draw_architecture_16x9()` | B15 |
| `charts/holdout_leaderboard_anim_final.png` | `hbar_spr()` | B10 |
| `charts/purged_walk_forward_timeline.png` | `timeline_spr()` at full content width | B06 |
| `code_screenshots/*.png` (5) | `code_spr()` with Pygments, at 1680 px column width | B04, B05, B06, B07, B09 |
| `agent_visualizations/phase1_agent_chain_16x9.png` | `agents_row()` — horizontal chain | B03 |
| `scenes/still_*.png` (16) | `render_frame()` | QA, thumbnails |
| `outputs/contact_sheet.jpg` | `contact_sheet()` | QA |

These are re-rendered at landscape dimensions, so they differ from the portrait cut's versions even where the content matches.

## Audio

| Asset | Source | Terms |
|---|---|---|
| `narration/scene_01…16.mp3` | edge-tts, `en-US-AndrewNeural`, rate +27 % — the same files as the 9:16 cut | Microsoft Azure neural TTS via the `edge-tts` client |
| `narration/narration_full.wav` | The 16 clips on the beat timeline, peak-normalised to −1 dBFS | Derived |
| Background music | None | Add your own licensed track at `assets/background_music.mp3` |

Sharing the voiceover files is what keeps the two cuts frame-accurate with each other. To re-record in your own voice, add `narration/scene_01_user.wav` … `scene_16_user.wav` to **both** folders and re-render each.

## Fonts

DejaVu Sans and DejaVu Sans Mono, bundled with matplotlib and distributed under a permissive licence that allows redistribution and embedding.

## Code shown on screen

Five panels quote your notebook: Cell 4 (`engineer_phase2_features`), Cell 5 (`MarketRegimeDetector.fit`), Cell 6 (`walk_forward_folds`), Cell 7 (`build_phase2_models`), Cell 10 (`build_ensembles`). Lines were re-wrapped to the column width; no identifier, argument or value was changed.

## Numbers

All metrics come from the printed output of Phase 2 Cells 3–14, mirrored in `data/phase2_results.json`, and are re-checked at render time.

## Third-party software

PyAV, Pillow, NumPy, pandas, matplotlib (fonts only), Pygments, edge-tts — all open-source packages from PyPI. None of their assets appear in the video beyond the fonts noted above.
