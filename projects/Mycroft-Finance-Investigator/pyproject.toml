[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mycroft-finance-investigator"
version = "0.1.0"
description = "Verified monthly performance investigation for Mycroft"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mycroft-finance-investigator = "mycroft_finance_investigator.cli:main"

[tool.setuptools]
packages = ["mycroft_finance_investigator"]
