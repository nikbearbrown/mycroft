[pytest]
# Keep console output clean for screenshots/demos: --no-header drops the
# platform/rootdir/plugins block (which otherwise leaks absolute machine paths
# and the Python executable path); no:cacheprovider avoids the .pytest_cache line.
addopts = --no-header -p no:cacheprovider
testpaths = tests
