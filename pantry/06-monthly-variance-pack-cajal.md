# CAJAL Figure Report — Chapter 6: Monthly Variance Pack

_Density: recommend 4 figures, [Mechanistic]._

## Figure 1 — Compute vs. Explain
Priority: Critical · Type: comparison panels

BLOCK 1 — ILLUSTRAE PASTE BLOCK:
Render a single-column two-panel comparison at 89mm width, 300 DPI, flat vector, white background, Okabe-Ito palette. The left panel, what the machine computes, holds four tokens — dollar variance, percent variance, rank by magnitude, flag by threshold, plus an attach-prior-comment mark — drawn in primary sky blue to read as deterministic and reproducible. The right panel, what the human explains, holds four tokens — causal explanation, materiality decision, accounting treatment, release authorization — drawn in bluish green to read as accountable judgment. Down the center, between the panels, draw one hard vertical line as the gate, the most prominent stroke in the figure, marking that the columns must stay separate. Keep the two panels equal in size to express that explanation is a distinct domain, not an extension of computation. Uniform 1pt strokes, no baked-in text, no shadows, no gradients.

BLOCK 2 — FULL SCOPE:
`[S - SPECIFICATION]` Single-column 89mm, ≥300 DPI, vector, Okabe-Ito.
`[C - CONTENT]` From "What a Variance Actually Is" diagram callout: left = what the machine computes (dollar variance, percent variance, rank by magnitude, flag by threshold, attach prior comment); right = what the human explains (causal explanation, materiality decision, accounting treatment, release authorization); a hard vertical gate line separates them.
`[O - ORGANIZATION]` Two equal panels left/right; one dominant central vertical gate line (no crossing arrows).
`[P - PRESENTATION]` Flat vector, Okabe-Ito hex, uniform 1pt strokes, white background, no baked text, no style suggestions; compute panel #56B4E9, explain panel #009E73, gate line #000000.
`[E - EXCLUSIONS]` Omit the six-ingredient inventory (Figure 2), the recipe-flow pipeline (Figure 3), the dual commentary-column schematic (Figure 4), the three supervision questions, the three-layer taxonomy, specific dollar figures ($420K, $310K).

BLOCK 3 — NEGATIVE PROMPT:
spreadsheet rows, numbers, charts, code, text labels, words, gibberish letters, titles, captions, decorative borders, realistic textures, drop shadows, gradient backgrounds, photographic elements, dual-headed arrows, hand-drawn styles, human figures, visual clutter, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion.

## Figure 2 — The Six Ingredients and Their Contracts
Priority: Important · Type: structural schematic

BLOCK 1 — ILLUSTRAE PASTE BLOCK:
Render a six-component ingredient schematic at 89mm width, 300 DPI, flat vector, white background, Okabe-Ito palette. Arrange six equal cells in a two-by-three grid: actuals, budget, forecast, mapping tables, thresholds, prior comments. Render the three required inputs (actuals, budget, mapping tables) in primary sky blue, the optional forecast in secondary orange, the human-set thresholds in dominant blue, and prior comments in reddish purple to mark it as context-not-explanation. Beside each cell place a small contract-marker token and a distinct vermillion stop-marker where a missing or wrong-versioned contract halts the run (actuals missing → stop; unmapped accounts above threshold → stop). Keep cells uniform in size with reserved interior whitespace for later captioning; bake in no text. Uniform 1pt strokes, no shadows, no gradients; group the cells under one shared frame so they read as the single ingredient set the pack requires.

BLOCK 2 — FULL SCOPE:
`[S - SPECIFICATION]` Single-column 89mm, ≥300 DPI, vector, Okabe-Ito.
`[C - CONTENT]` From "The Ingredients" table: six inputs — actuals (approved, versioned; missing → stop), budget (current version), forecast (optional), mapping tables (current crosswalk; unmapped above threshold → stop), thresholds (human-set before run), prior comments (context, labeled as prior, not explanation). Each input has a source/version/period/owner contract.
`[O - ORGANIZATION]` 2×3 grid under one frame; per-cell contract marker + stop-marker where applicable; no sequence arrows (parallel ingredients).
`[P - PRESENTATION]` Flat vector, Okabe-Ito hex, uniform 1pt strokes, white background, no baked text, no style suggestions; required inputs #56B4E9, forecast #E69F00, thresholds #0072B2, prior comments #CC79A7, stop markers #D55E00.
`[E - EXCLUSIONS]` Omit the compute/explain panels (Figure 1), the recipe-flow pipeline (Figure 3), the dual commentary schematic (Figure 4), the supervision questions, the multi-entity design exercise, specific dollar figures.

BLOCK 3 — NEGATIVE PROMPT:
spreadsheet gridlines, numbers, charts, code, text labels, words, gibberish letters, titles, captions, decorative borders, realistic textures, drop shadows, gradient backgrounds, photographic elements, dual-headed arrows, hand-drawn styles, human figures, visual clutter, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion.

## Figure 3 — What the Recipe Does, and Where It Stops
Priority: Critical · Type: process flowchart

BLOCK 1 — ILLUSTRAE PASTE BLOCK:
Render a left-to-right recipe pipeline at 89mm width, 300 DPI, flat vector, white background, Okabe-Ito palette. Begin with an ingredients-verified node, then five sequential operation nodes in sky blue: compute variances, rank by magnitude, flag by threshold, attach prior comments, leave blank fields for unsupported lines, ending in a structured work-surface node. After the work surface, place a distinct vertical gate element as the dominant interrupting stroke. To the right of the gate place a single human node in bluish green for commentary plus release. Mark the operation "stop" explicitly as a designed terminal of the machine sequence at the gate, not merely an absence of further nodes. Connect everything with single forward arrows; keep all machine nodes left of the gate and the human node right of it. Uniform 1pt strokes, no baked-in text, no shadows, no gradients.

BLOCK 2 — FULL SCOPE:
`[S - SPECIFICATION]` Single-column 89mm, ≥300 DPI, vector, Okabe-Ito.
`[C - CONTENT]` From "What the Recipe Does" diagram callout: ingredients verified → compute (dollar, percent, budget, forecast, prior-period) → rank by magnitude → flag by threshold → attach prior comments → blank fields for unsupported lines → structured work surface → gate → human commentary + release. The recipe stops at the gate; "stop" is a designed operation.
`[O - ORGANIZATION]` Linear left-to-right; single → arrows; one dominant vertical gate; machine nodes left, human node right.
`[P - PRESENTATION]` Flat vector, Okabe-Ito hex, uniform 1pt strokes, white background, no baked text, no style suggestions; machine operations #56B4E9, human node #009E73, gate #000000.
`[E - EXCLUSIONS]` Omit the ingredient inventory contents (Figure 2), the compute/explain panels (Figure 1), the dual commentary schematic (Figure 4), the supervision-question table, the three-layer taxonomy, specific dollar figures.

BLOCK 3 — NEGATIVE PROMPT:
realistic padlock, branching diamonds, numbers, charts, code, text labels, words, gibberish letters, titles, captions, decorative borders, realistic textures, drop shadows, gradient backgrounds, photographic elements, dual-headed arrows, hand-drawn styles, human figures, visual clutter, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion.

## Figure 4 — The Two Commentary Sub-Columns
Priority: Important · Type: structural schematic

BLOCK 1 — ILLUSTRAE PASTE BLOCK:
Render a paired-column schematic at 89mm width, 300 DPI, flat vector, white background, Okabe-Ito palette. Show a flagged-items list as a vertical run of identical row-blocks on the left, then two adjacent commentary sub-columns to their right. The first sub-column, prior commentary (sourced), is filled in reddish purple to mark it as dated prior context. The second sub-column, current explanation (owner required), shows a mix of filled bluish-green cells (where a named human has written) and deliberately empty cells (blank where no human has spoken). Make the empty cells a clear, intentional design element — blank is information. Draw a single small human-author marker beside each filled green cell to indicate named authorship; show no marker and no machine-generated fill in the empty cells. Keep row-blocks uniform; reserve interior whitespace for captioning; bake in no text. Uniform 1pt strokes, no shadows, no gradients.

BLOCK 2 — FULL SCOPE:
`[S - SPECIFICATION]` Single-column 89mm, ≥300 DPI, vector, Okabe-Ito.
`[C - CONTENT]` From "The Commentary Problem": two sub-columns replace one — "prior commentary (sourced)" and "current explanation (owner required)"; current column starts blank for every flagged item and is populated only by a named finance professional; the model never writes in it; a blank cell is information (no human has yet spoken). [Visibility = absence of a sentence where no human has spoken, per the text.]
`[O - ORGANIZATION]` Flagged-rows list left; two adjacent commentary sub-columns right; filled vs. intentionally-blank cells; author marker only on human-written green cells.
`[P - PRESENTATION]` Flat vector, Okabe-Ito hex, uniform 1pt strokes, white background, no baked text, no style suggestions; prior column #CC79A7, human-written cells #009E73, blank cells outlined neutral.
`[E - EXCLUSIONS]` Omit the recipe-flow pipeline (Figure 3), the ingredient inventory (Figure 2), the compute/explain panels (Figure 1), the supervision-question table, the multi-entity exercise, specific dollar figures.

BLOCK 3 — NEGATIVE PROMPT:
machine-generated placeholder text in blanks, spreadsheet gridlines, numbers, charts, code, text labels, words, gibberish letters, titles, captions, decorative borders, realistic textures, drop shadows, gradient backgrounds, photographic elements, dual-headed arrows, hand-drawn styles, human figures, visual clutter, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion.

## Video Candidate Pass

Figure 1 — STATIC SUFFICIENT — none — a two-domain contrast held by the central gate line; no temporal mechanism.
Figure 2 — STATIC SUFFICIENT — none — a parallel ingredient inventory; reference, not sequence.
Figure 3 — VIDEO CANDIDATE — ≥3 sequential causal stages — compute → rank → flag → attach → blank → work surface → halt at gate; an animated run that visibly stops at the gate dramatizes "the recipe stops; nothing crosses without a person."
Figure 4 — STATIC SUFFICIENT — none — the point is the persistent intentional blank cell; a still shows it best.

Video candidates: 1. Recommended for production: Figure 3 — an animated build of the five operations ending in an unmistakable halt at the gate reinforces the chapter's central design claim that "stop" is a designed behavior and the commentary column cannot be machine-filled.
