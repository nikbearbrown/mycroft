# Run Log

Use this file for meaningful recipe runs, blockers, generated artifacts, and
workflow changes.

## Template

```markdown
## YYYY-MM-DD -- Short task name

- **Recipe:** ...
- **Inputs:** ...
- **Commands:** ...
- **Outputs:** ...
- **Result:** ...
- **Open issues:** ...
```

## 2026-06-13 -- Bring Mycroft to Madison parity (instruction build + gate stack)

- **Skill:** Refactor Mycroft's agent context to the source-vs-adapter + enforced-gate architecture, reusing Madison's shared rule-module library.
- **Inputs:** Madison as template; Mycroft was earlier-stage (hand-written 18L/17L CLAUDE/AGENTS, no SNICKERDOODLE.md/DOMAIN.md/conformance.mjs).
- **Commands:** Ported `conformance.mjs` (SKIP mycroft-main), `to-markdown.mjs`, `build-instructions.mjs`. Added the constitution `SNICKERDOODLE.md` (the generic cross-domain one) + a new `DOMAIN.md` index. Vendored the 6 `_shared/` instruction modules; wrote `instructions/manifest.yml` (selects all 6 — it now has the backing tools/files) + `instructions/mycroft.md` (identity + Mycroft help menu: 99 recipes, 17 chapters, the gate scripts). Built + promoted root `AGENTS.md` (72L, generated) + `CLAUDE.md` (10L, `@AGENTS.md` import). Scaffolded `.claude/` hooks (archive-guard + conformance-check) + `.github/workflows/verify.yml` CI (conformance + drift guard). Updated package.json (verify/build-instructions/to-markdown) + .gitignore (build scratch + mycroft-main quarantine).
- **Outputs:** generated AGENTS.md/CLAUDE.md; SNICKERDOODLE.md, DOMAIN.md; instructions/ tree; ported scripts; .claude/ + .github/; package.json + .gitignore.
- **Result:** Mycroft now runs the same stack as Madison — generated instruction files (idempotent rebuild verified), conformance, hooks, CI drift guard. All checks pass. The two repos share the same `_shared/` module library (vendored per-repo so they can diverge); Mycroft's manifest selects all 6, Madison's the same 6 — proving the select-what-you-use design.
- **Open issues:** `_shared/` modules are vendored (one copy per repo) — kept in parity by hand for now; a shared-home/submodule is a later option if strict DRY is wanted. Mycroft has no prompts/ suites yet (content, not infra — not part of gate-stack parity).

## 2026-06-14 -- Research finance recipe opportunities

- **Recipe:** Research pass for entry- and mid-level finance recipe opportunities in Mycroft.
- **Inputs:** `SNICKERDOODLE.md`, `DOMAIN.md`, `DATA_CONTRACT.md`, `docs/recipes.md`, existing finance recipes and templates, plus current external grounding from BLS, SEC EDGAR API docs, FRED API docs, and PCAOB audit-evidence standards.
- **Commands:** Scanned existing finance recipe coverage with `find`/`rg`; reviewed representative recipes (`mycroft-financial-intelligence-hub`, `forecasting`); wrote a reusable deep-research prompt and the resulting research synthesis.
- **Outputs:** `reports/generated/entry-mid-finance-recipes-deep-research-prompt.md`; `reports/generated/entry-mid-finance-recipes-research.md`.
- **Result:** Identified highest-value gaps for finance practitioners: variance packs, budget-vs-actual commentary, reconciliations, close/flux analysis, AP/AR exception review, cash forecasting, KPI lineage, SEC filing comparison, covenant monitoring, audit binders, and CFO/board packet source checks.
- **Open issues:** These are research recommendations, not implemented recipes. Next pass can turn the top candidates into `recipes/` files and matching report templates.

## 2026-06-14 -- Add attached finance practitioner-map research

- **Recipe:** Add Bear's attached finance recipe-opportunity research to the Mycroft finance research corpus.
- **Inputs:** `/Users/bear/.codex/attachments/d6e90db6-64d5-4408-98a6-261e8381959f/pasted-text.txt`; existing `reports/generated/entry-mid-finance-recipes-research.md`.
- **Commands:** Copied the attached research into `reports/generated/mycroft-finance-recipe-opportunities-attached-research.md`; merged its most useful additions into the main finance synthesis.
- **Outputs:** `reports/generated/mycroft-finance-recipe-opportunities-attached-research.md`; updated `reports/generated/entry-mid-finance-recipes-research.md`.
- **Result:** Main report now includes the 22-recipe candidate map, occupational baseline, explicit do-not-automate list, additional recipe cards for budget requests/daily cash/control evidence/revenue billing, a stricter finance gate stack, concrete internal data contracts, and a revised build sequence.
- **Open issues:** Still research only; recipes and report templates have not yet been scaffolded.

## 2026-06-14 -- Rewrite TIKTOC for finance practitioner guide

- **Recipe:** Full TIKTOC architecture rewrite for Mycroft as a finance recipe engine.
- **Inputs:** Attached finance recipe opportunity research, attached Causal Reasoning TIKTOC template, existing Mycroft placeholder chapters, existing Mycroft finance recipes, `reports/generated/entry-mid-finance-recipes-research.md`, and `the-reallocation-engine/chapters` structure.
- **Commands:** Read attachments and existing chapter structure; compared against `the-reallocation-engine/chapters`; rewrote `TIKTOC.md` as a full architecture document with concept, learner profile, deployment, repo grounding, field positioning, act structure, chapter list, learning outcomes, running project, chapter anatomy, recipe strategy, risks, and open questions.
- **Outputs:** Updated `TIKTOC.md`.
- **Result:** `TIKTOC.md` now mirrors the reallocation-engine pattern: intro, chapters 1-5 framework, chapters 6-15 concrete finance practitioner recipes, chapter 16 honest run, and 97-99 appendices/back matter. It explicitly shifts the book from agentic investment intelligence toward entry/mid-level finance workflow recipes.
- **Open issues:** Current `chapters/01-chapter-01.md` through `chapters/12-chapter-12.md` are still placeholders and should be renamed or rewritten in a later chapter-writing pass.

## 2026-06-14 -- Gather research notes for missing finance chapters

- **Recipe:** Chapter Research Gatherer for the new Mycroft finance TIKTOC.
- **Inputs:** `TIKTOC.md`, finance research reports in `reports/generated/`, shared markdown library `/Users/bear/Documents/CoWork/bear-textbooks/MD`, official web grounding from BLS, SEC EDGAR API docs, FRED API docs, and PCAOB AS 1105.
- **Commands:** Extracted proposed named chapters 01-16 from `TIKTOC.md`; scanned 312 shared-library markdown files; copied 8 relevant `_lib_*.md` files into `pantry/`; generated chapter notes with `node scripts/generate-finance-chapter-research-notes.mjs`; inspected index and sample notes.
- **Outputs:** `pantry/chapter-research-index.md`; `pantry/01-the-fluency-trap_notes.md` through `pantry/16-the-build-and-the-honest-run_notes.md`; 8 `_lib_*.md` files; `scripts/generate-finance-chapter-research-notes.mjs`.
- **Result:** Every proposed named chapter now has a pantry research note covering TIKTOC summary, conceptual foundations, domain cases, dependencies, current field state, teaching considerations, and source references. The index records that the existing `chapters/01-chapter-01.md` style files are placeholders and the named chapter files remain missing.
- **Open issues:** Notes are research scaffolding, not chapter drafts. A later TIKTOC-driven writing pass should rewrite/rename the chapter files.

## 2026-06-14 -- TIKTOC-driven finance chapter write

- **Recipe:** Chapter Writer for Mycroft Finance Recipe Engine.
- **Inputs:** `TIKTOC.md`, `book.md`, pantry research notes, copied `_lib_*.md` files, finance research reports, and existing `chapters/97-fundamental-themes.md`.
- **Commands:** Read attached Chapter Writer prompt; confirmed named chapters from `TIKTOC.md` were missing while placeholder `chapters/01-chapter-01.md` style files still exist; generated named finance chapters with `node scripts/write-finance-tiktoc-chapters.mjs`; inspected representative chapter files and `logs/log.csv`.
- **Outputs:** Added `chapters/01-the-fluency-trap.md` through `chapters/16-the-build-and-the-honest-run.md`; rewrote `chapters/97-fundamental-themes.md` as a finance-specific appendix; added `scripts/write-finance-tiktoc-chapters.mjs`; appended chapter metadata to `logs/log.csv`.
- **Result:** The named Mycroft finance chapter set now mirrors the reallocation-engine style: framework chapters 01-05, concrete finance recipes 06-15, and an honest-run capstone in 16.
- **Open issues:** The older placeholder files `chapters/01-chapter-01.md` through `chapters/12-chapter-12.md` still exist and should be archived or superseded in a later cleanup pass if Bear approves.

## 2026-07-09 -- Add vendor-intelligence-brief recipe (Phase 1 scaffold)

- **Recipe:** vendor-intelligence-brief v0.1.0 (DRAFT) — structured vendor intelligence brief (6 sections, sourced).
- **Inputs:** User's existing vendor intelligence platform (5 specialized agents, supervisor routing, PostgreSQL signal storage, Neo4j competitive graph).
- **Commands:** 
  - Created `recipes/vendor-intelligence-brief.yaml` with full frontmatter, inputs/outputs, 5 phase gates (3 Phase 2 placeholders), architecture, and known issues.
  - Created `data/verified/ai_company_signals-schema.yaml` (signal table schema, validation rules, data quality notes, audit checks).
  - Updated `DATA_CONTRACT.md` to register vendor intelligence data layer + signal validation gate.
  - Logged this run in `logs/RUN_LOG.md` with status, blockers, and next steps.
- **Outputs:** 
  - `recipes/vendor-intelligence-brief.yaml` (DRAFT, 200L)
  - `data/verified/ai_company_signals-schema.yaml` (schema + validation rules)
  - Updated `DATA_CONTRACT.md` (vendor intelligence section)
  - This log entry
- **Result:** Mycroft vendor intelligence framework scaffolded at Phase 1 (DRAFT → SPECIFIED).
- **Blockers:**
  - [GATE OPEN] Signal validation (Phase 2) — zero signals validated; gate process not yet defined.
  - [GATE OPEN] Supervisor routing review (Phase 2) — Langfuse traces not logged to RUN_LOG.
  - [GATE OPEN] Brief approval (Phase 2) — no procurement owner review process.
  - [BLOCKER] Groq token limit at company #33 of 50 batch — blocks Phase 3 daily batch job.
- **Next steps:**
  - Phase 1 → Phase 2: Define signal validation gate (audit script, human sign-off process).
  - Phase 1 → Phase 2: Add gate decision logging to RUN_LOG (when running sample brief).
  - Phase 1 → Phase 2: Define brief approval gate (procurement owner + sign-off rule).
  - Phase 2: Run full sample brief (company: "Anthropic") with all gates open (no human decision yet, just logging).
  - Phase 2: Generate `signals-validation-audit.md` (spot-check 10 signals, assess quality).
  - Phase 3: Solve Groq token limit (upgrade tier or secondary provider for news classification).

## 2026-07-11 -- Hacker News AI Buzz Tracker: historical backfill, baselines, and watchlist-version guard

- **Recipe:** Hacker News AI Buzz Tracker (`data/raw/Hacker_News_AI_Buzz_Tracker/`) — historical backfill and trailing-baseline phase.
- **Inputs:** the project's 12-entity v1 watchlist config, existing scoring/parsing code from earlier phases (reused as-is, not rewritten), the Hacker News Algolia `search_by_date` API, Supabase (`hn_buzz_runs`, plus two new tables: a dedicated backfill table and an entity-baseline table).
- **Commands:**
  - Wrote and ran a 90-day historical backfill script (dry-run first, then live) — weekly chunked, paginated Algolia pulls, deduped by story ID, scored with the existing scoring code, inserted into the new dedicated backfill table.
  - Wrote and ran a trailing-baselines script against the backfilled data into the new entity-baseline table.
  - Diagnosed and fixed an Algolia query-matching bug (unquoted short terms, e.g. one entity's ticker-style term, matched hundreds of unrelated stories; some terms returned zero real hits) by quoting every query term and enabling exact-phrase matching, in both the backfill script and the live n8n workflow.
  - Fixing the live workflow required adding a separate quoted-term field threaded through the workflow's node schema, since an existing merge step matched the API's echoed query string against the original unquoted term field.
  - Migrated the live snapshot table to add a `watchlist_version` column and updated the live workflow's previous-run read query, row-builder, and insert step to close a gap: the version tag previously existed only as metadata on the backfill table, with no structural guard against a future watchlist-version comparison in the live velocity lookup.
  - Ran the live workflow manually post-migration to confirm the guard works without breaking velocity computation.
- **Outputs:** new backfill script and trailing-baselines script, updated live n8n workflow export, updated database setup documentation (new table schemas + migration note).
- **Result:** 13 weekly backfilled rows in the new backfill table (verified: full entity coverage per row, no duplicates, internally consistent confidence/score flags, query-matching fix holding at scale); one row per entity in the new baseline table; watchlist-version guard live and confirmed on a partial manual test run (velocity computed correctly, non-cold-start).
- **Open issues:**
  - [GATE OPEN] Full-watchlist live-run regression test of the watchlist-version guard — only tested with a subset of entities so far.
  - [BLOCKER] Backfilled engagement totals (points/comments) carry a look-ahead risk, since the source API returns current rather than point-in-time totals — unresolved; story count designated the primary metric for downstream analysis to route around it.
  - [GATE OPEN] Signal-validation backtest (pooled panel design with entity fixed effects, multiple-comparison correction, lead-lag test) specified but not yet implemented.

## 2026-07-16 -- Hacker News AI Buzz Tracker: signal-validation backtest (Week 6)

- **Recipe:** Hacker News AI Buzz Tracker (`data/raw/Hacker_News_AI_Buzz_Tracker/`) — Week 6 signal-validation backtest (early Phase 2).
- **Inputs:** Week 5 backfill (`backfill_output/backfill_v1.json`, 12 entities × 13 weeks, storyCount as primary point-in-time-safe metric); Alpha Vantage daily closes for the 9 public-ticker entities; `watchlist.json`.
- **Commands / artifacts:**
  - Installed and pinned the stats stack (`pandas`, `scipy`, `statsmodels`) in `requirements.txt`.
  - Wrote `docs/backtest_preregistration.md` **before** computing any correlation (lag family, metric, pooled-panel design, BH-FDR, bidirectional tests, weight-tuning decision rule, limitations all fixed up front).
  - Wrote and ran `fetch_prices.py` → `backfill_output/prices_v1.json` (9 tickers, 100 daily closes each, 2026-02-20 → 2026-07-15; rate-limit-aware + cached).
  - Wrote and ran `run_backtest.py` → `backfill_output/backtest_results.json` (pooled Pearson over 9 entities, 7-test lag family, BH-FDR q=0.05, both directions, per-entity descriptive).
  - Wrote `signal_validation.md` (the Week 6 deliverable report).
- **Result:** **Null result.** 0 of 7 pooled tests survive FDR. Forward buzz→price leads ≈0 / slightly negative; strongest raw cell is price→buzz at 2wk (r=0.23, p=0.027, q=0.19 — fails FDR), consistent with buzz reacting to, not leading, price. Investment framing downgraded to *unproven*, honestly, per the "building to learn" spirit.
- **Decision (logged):** Keep Buzz Score weights unchanged (30/30/20/20) per the pre-committed rule — no forward lead survived FDR with |r|≥0.20, so there is no validated target to tune toward; tuning would overfit a null. Re-evaluate at ≥26 weeks of backfill.
- **Open issues:**
  - [GATE OPEN] Human attestation (adequacy sign-off) of the backtest design and null verdict — machine conformance done (scripts run, JSON valid); human judgment pending.
  - Carried from Week 5: `watchlist_version` guard still missing on the live table (not blocking Week 6; must close before Week 11 v2 expansion).
  - Caveat: pooled Pearson p-values are optimistic under within-entity serial autocorrelation (bias runs downward, toward significance). The forward leads fail to clear even that lowered bar, so an autocorrelation-robust test would push the p-values up and leave them more clearly null — the bias runs against the null yet the null holds, which strengthens the verdict rather than threatening it. [Correction 2026-07-18: an earlier version of this caveat mis-stated the bias as "cutting toward the null"; the conclusion (verdict unchanged) was right but the reasoning was inverted — fixed here and in the report.]

## 2026-07-16 -- HN Buzz Tracker: Week 6 backtest attestation (gate cleared)

- **Recipe:** Hacker News AI Buzz Tracker — Week 6 signal-validation backtest.
- **Who/what/when:** Om Mali re-ran `run_backtest.py` independently (output matched the report exactly), hand-checked NVIDIA 2026-04-17 (storyCount 31 / NVDA close 201.68 against source files), and ran two break-checks (panel = 9 public entities; the 2026-07-03 holiday correctly falls back to the 2026-07-02 close). Adequacy sign-off and honest "did not test" list recorded in `docs/backtest_attestation.md`.
- **Result:** Human attestation gate **[GATE CLEARED]** by Om Mali, 2026-07-16. Null verdict accepted; keep-weights decision accepted; investment framing downgraded to *unproven* pending ≥26 weeks of backfill. This closes the [GATE OPEN] from the 2026-07-16 backtest entry above.

## 2026-07-16 -- HN Buzz Tracker: Week 7 LLM narrative layer

- **Recipe:** Hacker News AI Buzz Tracker — Week 7 LLM narrative/theme/tone layer.
- **Inputs:** project `GROQ_API_KEY` (Groq cloud, OpenAI-compatible endpoint); watchlist.json; live HN Algolia top stories; `metric_generation.parse_hit` (reused).
- **Commands / artifacts:**
  - Resolved the "free LLM" decision empirically: `GET /openai/v1/models` to list reachable models + a JSON-mode smoke test. Default `llama-3.3-70b-versatile`, fallback `llama-3.1-8b-instant`, Claude alt — documented in `docs/llm_narrative_design.md`.
  - Wrote `llm_narrative.py`: per-entity narrative/theme/tone via Groq (Claude path too), grounded prompt, controlled vocab, JSON mode, total graceful degradation.
  - Wrote `docs/llm_narrative_design.md` (model decision + prompt design + n8n integration steps for the user).
- **Result (verified):** Live NVIDIA → theme `controversy`, tone `neutral` (grounded in the GPU circular-financing story); offline GPT-5 fixture → `launch`/`neutral`; no-key path → `degraded=true` with reason. Unit checks passed: off-vocab enum coerced+flagged, prose-wrapped JSON parsed, garbage input → degrade. `py_compile` clean; workflow JSON unchanged and conformance-valid.
- **Open issues:**
  - [USER STEP] n8n workflow integration (add Groq credential + HTTP Request LLM node + rewire + persist `narratives`) is documented but must be applied and runtime-verified in the user's n8n — not testable from here (Pyodide code nodes have no network; needs live Postgres/SMTP). Steps in `docs/llm_narrative_design.md` §6.
  - Note: `ANTHROPIC_API_KEY` not set, so the Claude path is built but untested against the live Anthropic API.

## 2026-07-18 -- HN Buzz Tracker: Week 7 n8n LLM integration verified in n8n

- **Recipe:** Hacker News AI Buzz Tracker — Week 7 LLM narrative layer, live n8n verification (closes the [USER STEP] above).
- **What was applied (by Om Mali, in the running n8n):** Created a Groq Header-Auth credential; added `Explode Entities` (Code) → `LLM Narrative (Groq)` (HTTP Request, Body = "Using JSON" `{{ $json.groqBody }}`) → `Merge Narratives` (Merge, combine-by-position) → `Attach Narratives` (Code) → the three existing consumers; extended `Save Snapshot` INSERT to persist the `narratives` column.
- **Debugging notes (found during wiring):** (1) HTTP body must be "Using JSON", not "Using fields below" — a blank-named field yielded `{"error":"invalid syntax"}`. (2) This n8n build exposes neither an "Include Input Fields" toggle nor `_('Node')` in Python code nodes, so input (`entity`/`row`) is recombined with the Groq response via a Merge/combine-by-position node instead of passthrough. (3) Feeding only the single `topStory` gave a thin/misleading narrative; switched the prompt to the top 3 stories.
- **Result (verified):** Ran with NVIDIA enabled — `Attach Narratives` returned the full run row with the NVIDIA leaderboard entry carrying `narrative`/`theme`/`tone` (theme `controversy`, grounded in the Apple-overtakes-Nvidia + Chinese-alternatives stories) and a top-level `narratives` map; the row was written to `hn_buzz_runs` with a non-null `narratives` column. LLM narrative layer now confirmed working end-to-end in n8n.
- **Open issues:**
  - [OPTIONAL] Missing-key degrade path recommended to test once in n8n (bad Groq key → run completes with null narratives). Python-side degrade already verified 2026-07-16.
  - The repo's exported `Hacker News AI Tracker.json` still reflects the pre-Week-7 graph; re-export from n8n to sync the file (or defer to the Week 12 export deliverable).

## 2026-07-21 -- HN Buzz Tracker: Week 8 dashboard (build only, render/webhook unverified)

- **Recipe:** Hacker News AI Buzz Tracker — Week 8 dashboard.
- **Artifacts:** `dashboard/index.html` (static Chart.js: leaderboard, per-entity trend with watchlist-version boundary markers, narrative-theme breakdown, Community Opinion placeholder — colors restricted to the `brutalist/DESIGN.md` six-token palette); `dashboard/fixture_runs.json` (copy of `backfill_output/backfill_v1.json`, used for `?mock=1` local testing); `docs/dashboard_design.md` (design rationale + n8n `GET /webhook/dashboard` wiring spec, not yet applied to the exported graph).
- **Result:** JS syntax checked clean (`node -e` parse); `fixture_runs.json` passes `node scripts/conformance.mjs`. **Not verified:** actual browser rendering of the four panels (no browser tool in this session) and the live n8n webhook wiring (no running n8n instance here, and no `SUPABASE_URL`/`SUPABASE_ANON_KEY` configured in this environment's `.env`).
- **Open issues:**
  - [USER STEP] Open `dashboard/index.html?mock=1` in a browser and confirm all four panels render before this counts as done against the plan's "test the dashboard render" step; capture `preview.png` for the Month 2 milestone PR once confirmed.
  - [USER STEP] Apply the Webhook → Respond to Webhook wiring in `docs/dashboard_design.md` to the running n8n instance; re-export `Hacker News AI Tracker.json` (already deferred to Week 12 per plan.md).
  - Version-boundary marker logic only exercised against the "no boundary" case (fixture is all-v1); real test waits on Week 11's watchlist v2.
  - Carried open: `watchlist_version` guard missing on the live table (Week 5); this is unrelated to the dashboard and tracked separately.

## 2026-07-21 -- HN Buzz Tracker: Week 8 dashboard verified live (closes the [USER STEP]s from the earlier Week 8 entry)

- **Recipe:** Hacker News AI Buzz Tracker — Week 8 dashboard, live n8n + Supabase verification.
- **What was applied (Om Mali, in the running n8n/Docker/Supabase):** Built `Hacker News Dashboard.json` as a standalone workflow (Webhook -> Read/Write Files from Disk -> Extract from File -> Inject Config Code node -> Respond to Webhook); remounted `./dashboard` at `/home/node/.n8n-files/dashboard` in `docker-compose.yml` (the default-allowed path for file-read nodes); added `env_file: [.env]` + `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` so the Code node injects `SUPABASE_URL`/`SUPABASE_ANON_KEY` from the container env rather than hardcoding them.
- **Bugs found and fixed during wiring (full detail in `docs/dashboard_design.md`):** malformed `={{ }}` response expression; file-access sandbox restriction; `$env` blocked by default; a `.replace()`-first-match-only bug that silently left a literal `%%SUPABASE_URL%%` token as the "real" value; a `service_role` key mistakenly wired in place of `anon` (caught before shipping, rotated in Supabase); a `navigator.locks` `SecurityError` from `supabase-js`'s default auth-lock behavior (fixed with a no-op `lock` override); `community_opinions` requested in the dashboard's query before that column exists on the live table (dropped from `.select()` until Week 9 adds it).
- **Result (verified):** `GET http://localhost:5678/webhook/dashboard` returns the live, key-injected dashboard HTML; confirmed rendering in a browser against real `hn_buzz_runs` data (leaderboard, trend chart, theme breakdown/placeholder, Community Opinion placeholder).
- **Open issues:**
  - [USER STEP, recommended] Explicitly confirm the `anon`-role RLS policy on `hn_buzz_runs` rejects writes, not just that reads succeed — the anon key is visible in the page's source, so RLS is the only thing keeping that safe.
  - `preview.png` screenshot and the Month 2 milestone PR (Weeks 5-8) are still open — not done as part of this entry.
  - Carried from Week 5: `watchlist_version` guard still missing on the live table.

## 2026-07-22 -- HN Buzz Tracker: Week 9 Community Opinion analyzer (build + LLM smoke-test; n8n wiring/migration documented, not yet applied)

- **Recipe:** Hacker News AI Buzz Tracker — Week 9 Community Opinion analyzer (`data/raw/Hacker_News_AI_Buzz_Tracker/`).
- **Artifacts:** `community_opinion.py` (HN Firebase comment fetch, HTML-cleaning/dedupe, per-entity LLM opinion analyzer, cross-entity sector-theme clustering — mirrors `llm_narrative.py`'s Groq/Claude plumbing); `tests/test_community_opinion.py` (12 fixture tests, no network); `docs/community_opinion_design.md` (fetch strategy, token budget, prompt design, degrade table, `community_opinions` column migration SQL, n8n node-wiring spec mirroring `docs/llm_narrative_design.md`).
- **Result:** `tests/test_community_opinion.py` 12/12 passed. `python community_opinion.py --demo` hit this environment's real `GROQ_API_KEY`, got an API error on that call, and returned a clean `degraded=true` record — degrade contract confirmed live, not just in fixtures. `python community_opinion.py --sector --demo` succeeded against the real key and returned a genuine clustered sector narrative from two fixture entity opinions.
- **Not verified / open:**
  - [USER STEP] `alter table hn_buzz_runs add column if not exists community_opinions jsonb` — must be applied to the live Supabase table (it predates this column; same gap that caused the Week 8 dashboard's `column does not exist` error).
  - [USER STEP] The n8n node wiring in `docs/community_opinion_design.md` §8 (comment-fetch loop, Clean Comments Code node, Community Opinion + Cluster Sector Themes HTTP nodes, Save Snapshot INSERT extension) — needs applying and verifying against the running n8n instance, same pattern as Weeks 7-8.
  - Live comment-fetch path (`fetch_comments_for_story`) untested against a real entity this session — only LLM/parsing/degrade layers were smoke-tested.
  - Dashboard's Community Opinion panel (Week 8) remains a placeholder until this pipeline is live; wiring it is separate follow-up work.

## 2026-07-25 -- HN AI Buzz Tracker — Weeks 10-12 (attribution fix, digest, contract, config, launch)

- **Recipe:** Hacker News AI Buzz Tracker — Weeks 10-12 (`data/raw/Hacker_News_AI_Buzz_Tracker/`).
- **Constraint:** did NOT modify `Hacker News AI Tracker.json` (read-only for understanding); all code changes in `node code/` + reference modules + docs, per standing instruction.
- **Week 10 (verified):** `node code/Get Metrics.py` now relevance-filters top-story selection (query term must be in the story TITLE) — fixes the Bento misattribution where a name-drop Show HN topped OpenAI & Anthropic. Metrics stay over all hits; only narrative/opinion attribution is gated. New reference `story_relevance.py` + `tests/test_story_relevance.py` (8 fixtures incl. the real Bento case). `docs/relevance_fix_design.md`. Richer digest in `node code/Code in Python1.py` (sector narrative + badges + per-entity Community Opinion). `docs/alert_logic.md`.
- **Fixed pre-existing drift:** `compute_buzz_score.py` reference was Week-3 era (no ACCEL_MIN/MAX/velocity) and broke `tests/test_scoring.py` collection → synced to `node code/Computer Buzz Score.py`; converted `test_scoring.py::test_acceleration` return→assert.
- **Week 11:** `docs/json_signal_contract.md` (schema 1.0 formalized + additive 1.1 + `/webhook/signal` spec); `config/watchlist.v1.json` (conformance ✓) + `docs/watchlist_governance.md` (version guard status + v2 protocol as a human gate, not activated).
- **Week 12:** README signals/architecture updated; `.env.example` + SMTP/service_role; `docs/launch_article.md`.
- **Commands:** `python -m pytest tests/` → **21 passed, no warnings**; `python tests/test_scoring.py` → 5 passed; digest smoke-tested with sample row; `node scripts/conformance.mjs config/watchlist.v1.json` → conform.
- **Not verified / open (human/canvas):** paste updated Get Metrics + digest into live n8n and re-verify a full run (confirm Bento no longer tops OpenAI/Anthropic; check `relevantStories`); wire `/webhook/signal` + Error-Trigger failure alert; SMTP creds; watchlist v2 approval + lockstep writer/reader edit; re-export `workflow.json`; record demo; open final PR.

## 2026-08-08 -- Private AI Valuation Agent: Week 1 feasibility verification

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 1 — reproduce the verification, scaffold, lock universe v1).
- **Inputs:** SEC EDGAR `primary_doc.xml` for 8 NPORT-P accessions across 6 fund families (Fidelity 0000024238, T. Rowe Price 0000819930, Alger 0000003521, ARK 0001905088, BlackRock 0000887509, Capital Group 0000719608), period ends 2026-03-31 through 2026-05-31.
- **Commands:** Hand transcription from EDGAR by Om Mali; `python scripts/verify_week1_marks.py`.
- **Outputs:** `data/raw/Private_AI_Valuation_Agent/` — `docs/feasibility.md`, `docs/worklog.md`, `README.md`, `.gitignore`, `.env.example`, `requirements.txt`, `scripts/verify_week1_marks.py`, `tests/fixtures/week1_verified_marks.json`, `src/` package scaffold.
- **Result:** Core hypothesis supported by first-party evidence. 19 Anthropic PBC positions verified; four independent managers priced Anthropic identically to the cent ($259.14 / $259.1364) at 2026-03-31 and 2026-04-30, and after a repricing BlackRock (5/29) and Capital Group (5/31) both marked $589.0095. Staggered fiscal quarter-ends confirmed across four distinct observation dates, so propagation is observable. Write-up-all-classes convention (Gornall & Strebulaev) reproduced on this cohort, including common and preferred at an identical mark.
- **Open issues:** **Defect in `plan.md` lines 106/400** — the specified private-position filter (`IS_RESTRICTED_SECURITY='Y' AND FAIR_VALUE_LEVEL=3 AND CUSIP='N/A'`) keeps only 1 of 6 managers: filers use `000000000` as well as `N/A` for the CUSIP placeholder, and ARK and Capital Group report `isRestrictedSec='N'` on restricted stock. `FAIR_VALUE_LEVEL=3` was the only field constant across all 19 rows; replacement filter proposed in `docs/feasibility.md` §2, recall 19/19, **precision untested**. Three further factual errors in `plan.md` logged in `docs/feasibility.md` §6. Whether the DERA bulk TSVs preserve the raw XML placeholder values is untested and is the largest remaining risk. Universe v1 not frozen — human gate still open.

## 2026-08-08 -- Private AI Valuation Agent: bulk N-PORT verified against filings

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 1 de-risking — does the DERA bulk data behave like the raw XML?).
- **Inputs:** `2026q2_nport.zip` (440.7 MB, 32 files, 5,347,869 holding rows across 14,416 submissions); `tests/fixtures/week1_verified_marks.json` (19 hand-verified Anthropic positions).
- **Commands:** `python -m src.ingest.download_bulk 2026q2 --extract`; `python scripts/check_bulk_vs_xml.py data/2026q2_nport`.
- **Outputs:** `src/ingest/download_bulk.py`, `scripts/check_bulk_vs_xml.py`, `data/2026q2_nport/` (gitignored), updated `docs/feasibility.md` §5-§9 and `docs/worklog.md`.
- **Result:** **Bulk matches raw XML exactly — 15/15 rows, 0 mismatches.** `N/A` and `000000000` CUSIP placeholders both preserved verbatim; ARK's incorrect `IS_RESTRICTED_SECURITY='N'` passed through unrepaired. The largest open feasibility risk is closed. Universe measured across the full quarter: Databricks 360 rows/152 funds, SpaceX 154/69, Anthropic 149/84, OpenAI 142/73, Anduril 130/50, Cerebras 37/28; Perplexity 7, Groq 3, Scale AI 0 (drop confirmed). Two name-match false positives caught: `%COHERE%` returns 1,094 rows of **Coherent Corp** (public, 0 at Level 3) and `%X.AI%` is mostly bank debt and 144A bonds priced per $100 face.
- **Open issues:** **New defect — filter precision.** `plan.md` line 401 predicts "a few thousand" rows out per quarter; measured 606,028 (11.3% of the quarter) for the plan's filter and 693,951 for the corrected one. `CUSIP='N/A'` is the generic no-CUSIP placeholder (1.17M rows, 21.9%), not a private-company marker. **Decision: invert the pipeline** — name-match the universe first, confirm with `FAIR_VALUE_LEVEL=3` second, reducing entity-resolution input from ~600k rows to ~1k/quarter. Also measured: 5.35M rows/quarter, not the 10-15M in `plan.md` line 214; 985 Level-3 rows have zero balance; SpaceX spans $204.64-$5,265.90 in one quarter (unit artifact confirmed). Universe v1 still not frozen — human gate open; recommendation is six companies plus SpaceX added and Cohere dropped.

## 2026-08-08 -- Private AI Valuation Agent: universe v1 frozen (GATE)

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 1 — freeze universe v1 and write the selection criteria).
- **Gate decision:** universe v1 **frozen** by **Om Mali**, 2026-08-08.
- **Inputs:** coverage measured across all 5,347,869 holding rows of 2026Q2 (`docs/feasibility.md` §8); field definitions confirmed against `nport_readme.htm` bundled in the bulk zip.
- **Outputs:** `universe_v1.json` (machine-readable, with per-company rationale), `docs/feasibility.md` §8, `docs/worklog.md`.
- **Result:** Full members (6): Databricks, SpaceX, Anthropic, OpenAI, Anduril, Cerebras. Carried thin (1): Figure AI — marks published, dispersion and propagation suppressed. Excluded (5): Cohere (false positive — all 1,094 rows are Coherent Corp, a public NYSE company, 0 at Level 3), xAI (instrument mismatch — only 7 of 58 rows are Level 3 equity; the rest is bank debt and 144A bonds priced per $100 face), Perplexity (7 rows / 6 funds) and Groq (3 rows / 3 funds) for insufficient coverage, Scale AI (0 rows). Criterion: 28+ distinct filers per quarter, judged on Level 3 **equity** rows only. Two changes from `plan.md`'s universe — **SpaceX added** (69 funds, better coverage than four listed companies) and **Cohere removed**. Cerebras retained despite being the smallest full member because its Level 3 → Level 1 IPO transition is the project's only external validity check.
- **Open issues:** Schema check surfaced that **every field in `FUND_REPORTED_HOLDING` is nullable, including `FAIR_VALUE_LEVEL`** — the one field the corrected filter depends on — so `NULL` must be handled explicitly rather than assuming `= '3'` is well-defined. `ISSUER_CUSIP` is free-text `VARCHAR2(9)`, not a validated CUSIP. Only 2026Q2 is loaded; no cross-quarter movement observed. Supabase provisioned, no schema created. 2026Q1 not downloaded (Q2 was chosen because it contains the hand-verified May/June filings).

## 2026-08-15 -- Private AI Valuation Agent: Week 2 bulk ingestion, 14 quarters loaded (GATE)

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 2 — bulk ingestion at scale).
- **Gate decision:** Week 2 ingestion **cleared** by **Om Mali**, 2026-08-15 — recorded on his explicit instruction to decide on his behalf; he did not personally inspect the reconciliation tables. Logged as a delegation, not a review, so the audit trail is not misleading (P1).
- **Inputs:** DERA bulk N-PORT 2023Q1–2026Q2, 14 quarters, 5.9 GB of zips (2019Q4–2022Q4 deliberately not ingested — `docs/feasibility.md` §9 concluded the AI cohort barely existed in fund portfolios before ~2023, and 2023Q1 confirms it at 187 universe rows against 948 in 2026Q2).
- **Commands:** `python -m src.ingest.download_bulk <q>` ×13; `python -m src.ingest.build_parquet --all`; `python -m src.db.connect`; `python -m src.db.load --all` (twice, for the idempotency test); `python scripts/reconcile.py --db`; `python scripts/check_fund_complexes.py`; `pytest -q`; `node scripts/conformance.mjs`.
- **Outputs:** `src/ingest/universe.py`, `src/ingest/build_parquet.py`, `src/db/{schema.sql,connect.py,load.py}`, `scripts/reconcile.py`, `scripts/check_fund_complexes.py`, `tests/test_week2_ingest.py` (23 tests), `DATABASE_SETUP.md`, `data/parquet/<q>/{private_holdings,universe_holdings}.parquet` + 14 committed `reconciliation.json` provenance records; updated `README.md`, `docs/worklog.md`, `.gitignore`.
- **Result:** **80,571,213 source rows → 22,041,937 private → 5,806 universe → 5,806 `raw_holdings`**, reconciling exactly at all four boundaries across 14 of 14 quarters. Supabase PostgreSQL 17.6: 183 funds, 1,512 filings. Idempotency demonstrated against the live database — a second `load --all` inserted 0 and skipped 5,806 with every total unchanged. The Week 1 anchor reproduces **from the pipeline, not the fixture**: 43 distinct CIKs at $259.14 against six verified by hand. Stagger confirmed across **12 of 12 calendar months**, which is the basis of the propagation claim. Anthropic series now spans 33 period ends, 2023-04-28 → 2026-04-30, $11.79 → $388.19. The four complexes `plan.md` left unverified are answered: **Neuberger Berman (133 rows, 4 CIKs) and Morgan Stanley (103, 4) are real holders; Baillie Gifford and Invesco have none** — plus 31 complexes Week 1's full-text search missed (Baron Capital 324 rows, Lincoln Financial 149, SunAmerica 110, MassMutual 90, BNY Mellon 79, Brighthouse 76, StepStone 55, Coatue 36). 23 tests pass; conformance clean on 15 JSON files.
- **Findings that changed the design:** (1) **The DERA sets are indexed by FILING quarter, not period.** With the ~56-day lag the newest as-of date in 2026Q2 is **2026-04-30**, so the $589.0095 Anthropic repricing verified in Week 1 is **absent from bulk entirely** (nothing exceeds $318.57); it sits in 2026Q3, unpublished (`2026q3_nport.zip` → HTTP 404). This makes Week 3's live-EDGAR path the only route to current marks, not an optimization. (2) **Two date defects fixed:** DERA dates are `DD-MON-YYYY` text that sorts alphabetically (`30-APR` before `31-MAR`), which would have silently inverted propagation lag; and `REPORT_ENDING_PERIOD` is the fund's **fiscal year end**, not a quarter end — `REPORT_DATE` is the correct `period_end` (BlackRock Funds files FY-end 31-MAY-2026 carrying holdings as of 27-FEB-2026). (3) **Fidelity's `Variable Insurance Products Fund I–IV` were counting as four independent managers**, inflating exactly the count cross-manager dispersion depends on; Fidelity consolidates from 15 to 19 CIKs. (4) **`ASSET_CAT` must be an allow-list** (`EC`/`EP`/`OTHER`/NULL): `LON` rows are Databricks term loans at Level 3 with balance == value that would price at ~1.00, but `OTHER` holds *real* marks (`ANTHROPIC` 964,742 sh / $249,999,768.80 = $259.1364), so an EC/EP-only rule would discard genuine data. (5) `%OPENAI%` misses BlackRock's `Open AI Group PBC`; `%SPACE EXPLORATION%` misses `SPACEX-CL A PP`.
- **Open issues:** **Amendments are loaded beside their originals, unadjudicated** — 16 `NPORT-P/A` filings contributing 71 `raw_holdings` rows; correct for an append-only raw layer, but any mark built before Week 7 resolves them would double-count those fund/periods. **The SpaceX 10x sits inside a single filing, not across managers** — Baron accession `0001752724-24-195357` reports EC at $112.00 and EP at exactly $1,120.00, with three issuer spellings including the filer's typo `SPACE EXPLORATION TECHNOLOGICS`; 309 of 624 SpaceX fund/period groups show a ratio of exactly 10.000 and no other company shows it at all. All 645 SpaceX `EP` rows are above $500 (`EP` is a sufficient high-band signal, zero exceptions) but 125 of 1,005 `EC` rows are also high, all from Neuberger Berman tagging preferred as common — so Week 7's detector may trust `EP` but must not read `EC` as evidence of common. Deliberately left unadjusted per the never-auto-adjust rule. Also open: the private-row count oscillates by quarter (2023Q1 2.32M vs 2026Q2 694k, q1/q3 above q2/q4) and is unexplained; two Anthropic period ends show 161% (2025-07-31) and 80% (2025-12-31) spreads pending the split detector; OpenAI's range starts at $1.00; Morgan Stanley's `Discovery Portfolio` files two `NPORT-P` with the same period end on the same day; `fund_name` is not unique across `fund_id` (`(cik, series_id)` is the real key); 2019Q4–2022Q4 remain un-ingested.

## 2026-08-22 -- Private AI Valuation Agent: Week 4 golden set + deterministic matcher baseline

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 4 -- golden set and baseline metrics; plus the deterministic half of Week 3, which Week 4's own deliverable depends on).
- **Gate decision:** **NOT cleared -- human attestation on the golden-set labels is OPEN.** The 322 labels are agent-assigned by the documented evidence procedure in `scripts/label_golden_set.py`; `tests/fixtures/golden_set_v1.json` carries `human_attestation: null` and `tests/test_resolve.py::test_the_human_attestation_gate_is_recorded_as_open` asserts it stays null. Unlike the Week 2 reconciliation, a wrong label here is not mechanically detectable and silently biases every future matcher metric, so it was not self-certified on delegation. The reviewable surface is the **10 adjudications in section 6 of `docs/entity_resolution.md`**, not 322 rows: 108 labels come from registered LEIs, 100 from a name containing the company's own name, 78 from nothing firing.
- **Inputs:** `data/parquet/*/{private_holdings,universe_holdings}.parquet` -- 22,041,937 private holdings, 3,204,853 distinct `ISSUER_NAME` strings, 5,806 universe holdings, 14 quarters 2023Q1-2026Q2.
- **Commands:** `pip install rapidfuzz==3.11.0`; `python -m scripts.build_golden_candidates`; `python -m scripts.label_golden_set`; `python -m scripts.score_matcher`; `python -m pytest tests/ -q` -> **89 passed**; `node scripts/conformance.mjs` on 10 new artifacts -> all conform.
- **Outputs:** `src/resolve/normalize.py`, `src/resolve/match.py`, `scripts/build_golden_candidates.py`, `scripts/label_golden_set.py`, `scripts/score_matcher.py`, `tests/test_resolve.py` (56 tests), `tests/fixtures/golden_set_v1.json` (322 strings / 7,276 holdings), `docs/entity_resolution.md` (part 1, sections 1-8), `docs/_golden_candidates.json`, `docs/_matcher_metrics.json`; updated `docs/worklog.md`. `requirements.txt` unchanged -- rapidfuzz was already pinned for weeks 3-4.
- **Result:** Two systems scored on the same labels. **Frozen Week 2 LIKE patterns: P 0.9873 / R 0.9791** (3 false positives, 5 false negatives). **Matcher v1 (LEI -> alias -> SPV unwrap -> gated fuzzy): P 0.9958 / R 1.0000**, one false positive; per-holding P 0.9998 / R 1.0000. Net: **85 wrongly-missed holdings and 33 wrongly-included ones removed, one wrongly-included one introduced.** On the 144-string "hard" subset -- entries whose label rests on something other than the name resembling the answer -- v1 is P 0.9929 / R 1.0000 against the patterns' P 0.9928 / R 0.9928. Share-class grammar agrees with the filer's own `ASSET_CAT` on **89.47%** of 4,045 comparable holdings; within-cell price agreement is **68.3%** of 694 multi-filer cells.
- **Findings:** (1) **`%X.AI%` misses Fidelity's `XAI CORP` -- 85 holdings, and Fidelity is X.AI's largest holder.** The literal dot; closed by deleting dots inside alphabetic runs. Largest recall gap in the shipped pattern set. (2) **`Anthropics Technology Ltd., Series G` is Anthropic PBC.** A real British software company's name, filed by BlackRock, priced at 259.13640004 and 259.13641638 on 2026-03-31 -- the period end on which 14 LEI-confirmed holdings across 10 registrants price Anthropic Series G at 259.1364. `INVESTMENT_COUNTRY` GB is inherited from the wrong entity in a security master. (3) **231 OpenAI holdings across 7 title spellings have a price that is not a share price** -- `CONVERTIBLE INTEREST RT`, `RTS 1:1 @ USD`, `EV UNITS`, profit-participation units -- balance equals value, so they price at exactly $1.00. OpenAI's universe range runs $1.00 to $769.43 and the bottom of it is an artefact; `price_basis` is now a first-class label. (4) **`ANDURIL ENGINEERING LLC` and `ANDURIL INVESTORS LLC` are Anduril Industries**, BlackRock vehicles priced at the Anduril consensus to four decimals across nine period ends. (5) **`OPENAI FOUNDATION` is OpenAI Group PBC equity** filed under the non-profit parent's name -- Franklin twice and Nuveen, both at 687.69. (6) **World Labs C vs C Prime confirmed with prices**: $314.76-315.31 against $337.66-338.23, same ten funds, same period ends. (7) **All 14 near-integer price cells are SpaceX at exactly 10.00x and all have a parsed class of `UNKNOWN`** -- the precise diagnosis of the Week 2 10x finding: when the title omits the class, the 10x preferred and the common collapse into one cell.
- **Design conclusions that came from measurement, not assumption:** **`token_set_ratio` cannot be the deciding scorer** -- it scores `FIGURE` vs `FIGURE AI` at 100, `OPEN BAY AUTOS AI` vs `OPEN AI` at 100, and `ANDURIL ENGINEERING` vs `ANDURIL` at 100, two wrong and one right, all at the top of the range; hence a token-coverage gate evaluated before any score. An IDF weighting was built first and abandoned on measurement: **79% of the corpus's normalised tokens are hapax** (DF median 1, p99 6), so IDF cannot separate distinctive from generic here. **The operating point is a band, not a threshold**: the only false positive (`OPEN BAY AUTOS AI INC.`, a used-car marketplace whose name contains OPEN and AI in that order -- the whole of the two-token `OPEN AI` alias) scores **0.80**, and so do three *correct* blended SpaceX SPVs. Nothing separates those four. Recommended: auto-accept >= 0.90, review 0.80-0.90 (**4 issuer strings per full 14-quarter re-resolution**), reject below -- which is the concrete input to Week 6's queue.
- **Defects found in my own evidence pipeline before it was trusted:** Databricks term loans are tagged `LON` with balance == value, pricing at exactly 1.00, so with them in the price anchor every unrelated issuer priced at 1.00 "confirmed" as Databricks; and one Databricks row prices at **-0.005**, which made a ratio-tolerance test pass for anything at all because the denominator was negative. Both fixed with an instrument filter (`_is_share_price`). **The sampling frame was not reproducible** -- rebuilt from scratch it produced 322 strings on one run and 323 on the next, which invalidates a fixture. Three ordering causes: DuckDB's `DISTINCT` gives no ordering guarantee so the 3.2M-name candidate list arrived differently each run; `process.extract` breaks equal scores by input position, propagating that order into which equal-volume name became a stratum representative; and `price_evidence` iterated a *set* of company names, letting `PYTHONHASHSEED` decide which anchor got credited for a price coincidence. Fixed with an explicit `ORDER BY`, a name tie-break and a sorted list; the frame now rebuilds byte-identically (sha256 verified across two runs) and `test_the_fixture_still_matches_the_frame_it_was_built_from` re-derives every label from the committed frame. No metric moved -- the varying string was a near-miss negative -- but the count was 323 until this was found and is 322 now. Separately, **two of my own test assertions were wrong and the tests caught them**: `token_set_ratio('RELATIVITY SPACE','SPACE EXPLORATION')` is 66.67, not 100 (the `match.py` docstring was corrected along with the test), and blocking returns 2 candidates for `PUBLIC JOINT STOCK COMPANY PHOSAGRO` on the trigram `GRO` it shares with GROQ, not 0 -- blocking is recall-preserving and is supposed to let that through for the scorer to reject.
- **Deviations from `plan.md` Week 4, stated rather than absorbed:** (a) The unit of labelling is the distinct (name, title) pair, not the holding. The 5,806 universe holdings contain only 231 distinct pairs, so a random sample of 250 holdings would be ~95% repeats of `DATABRICKS INC`; each entry carries its holding count so metrics are reported both macro and micro. (b) 322 strings, above the plan's 200-300 band, left there rather than tuning a sampling parameter to hit a round number. (c) **Week 3 was not done in full** -- the deterministic matcher (normalisation, LEI short-circuit, blocking, `rapidfuzz`, per-filer title grammar) was built because Week 4's deliverable requires it, but the **live EDGAR current-quarter discovery and `primary_doc.xml` parser were not**, so no current-quarter filing has been through this matcher. (d) **Milestone PR 1 not opened** -- no git add/commit/push was performed; the working tree is left for review.
- **Open issues:** **Human attestation on the golden set (above).** A name that contains a company's name but is a different company will resolve inside the auto-accept band -- a hypothetical `ANTHROPIC MINING CORP` would land at 0.90; nothing in the corpus does this today, and it is the specific gap Week 5's adjudication layer must be measured against. `CLASS <letter>` -> common is a real approximation: Baron's 12 X.AI `CLASS B`/`CLASS C` holdings are tagged `EP` by the filer and are probably preferred; kept because the rule is price-confirmed for SpaceX's 100-plus common holdings and because X.AI's `CLASS`-titled rows report identical prices to its others in every overlapping period, so it changes no mark -- but it needs a per-company override before Week 7. The de-dotting rule mangles website-style names (`OPENAIR.COM` -> `OPENAIRCOM`); harmless here, not harmless in general. **206 of the 220 disagreeing price cells are assumed to be genuine cross-manager dispersion and have not been individually examined** -- if some are parsing failures the 68.3% figure moves. Carried forward unchanged from Week 2: 16 `NPORT-P/A` amendments (71 holdings) still unadjudicated and still required before Week 7; the oscillating private-row count; 2019Q4-2022Q4 un-ingested. No figure was produced for this week.

## 2026-08-22 -- Private AI Valuation Agent: golden-set attestation (GATE) + OPENAIR.COM correction

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 4 -- human gate on the golden-set labels).
- **Gate decision:** **PARTIALLY cleared** by **Om Mali**, 2026-08-22. Reviewed `docs/entity_resolution.md` section 6 (the adjudications) and confirmed **8 of the 10 labelled strings, covering 68 holdings**. Confirmed merges: `ANTHROPICS TECHNOLOGY LTD.`, `ANDURIL ENGINEERING LLC`, `ANDURIL INVESTORS LLC`, `SPACE EXPLORATION TECHNOLOGICS`, `OPENAI FOUNDATION`, `ANTHROPIC, INC.` Confirmed not-in-universe: `OPEN BAY AUTOS AI INC.` **Withdrawn same day:** `OPENAIR.COM` (below). **Not individually named in the review:** `XAI CORP.`, left as labelled and recorded as unnamed rather than folded into the approval. **314 of the 322 labels remain unattested**, including all 108 LEI labels, all 100 self-name labels and all 78 negatives. Attestation recorded in `tests/fixtures/golden_set_v1.json` -> `human_attestation`, with an explicit `scope` field naming what was not reviewed, and `voids_on` naming what invalidates it.
- **Commands:** `python -m scripts.label_golden_set`; `python -m scripts.build_golden_candidates`; `python -m scripts.score_matcher`; `python -m pytest tests/ -q` -> **101 passed**; `node scripts/conformance.mjs` on 13 artifacts -> all conform.
- **Outputs:** `src/resolve/normalize.py` (dot-TLD strip), `scripts/label_golden_set.py` (attestation record + corrected adjudication), `tests/test_resolve.py` (+6 tests), `tests/fixtures/golden_set_v1.json`, `docs/_golden_candidates.json`, `docs/_matcher_metrics.json`, `docs/entity_resolution.md` (sections 2, 6, 7, 8 rewritten), `README.md`, `docs/worklog.md`.
- **Result: the review found a real error in the one adjudication that asserted there was nothing to see.** `OPENAIR.COM` was labelled `NOT_IN_UNIVERSE` with the written reason that its price coincided with an OpenAI anchor "in exactly one period, and a single-period coincidence between unrelated issuers is ordinary." **That statement was false.** The rows: five holdings, **two** distinct period ends, all priced **687.6869**, titled **`OpenAir.com, Series C`**. 687.6869 is the OpenAI Series C consensus -- eight holdings across six registrants whose identity is not in dispute report it to four decimals on 2026-03-31, one carrying OpenAI's registered issuer identifier and seven naming OpenAI outright. (Corrected 2026-08-23: these were first described as eight LEI-confirmed holdings, which was wrong -- only one carries the LEI.) Filers are BlackRock (three holdings) and New York Life (two). Every issuer priced at 687.69 in those two periods is an OpenAI spelling. Corrected label: **`OpenAI Group PBC`**, same reasoning as `ANTHROPICS TECHNOLOGY LTD.` and `OPENAI FOUNDATION` -- a security-master collision with a similarly-named company, caught by price. The approval rested on the false statement, so it is recorded as withdrawn rather than carried over (P6: no artifact silently wins), and `test_the_withdrawn_attestation_is_not_counted_as_confirmed` asserts the withdrawn name never appears among the confirmed ones.
- **Matcher defect exposed by the same row, and fixed:** de-dotting fuses a domain suffix into the stem, so `OPENAIR.COM` became the single token `OPENAIRCOM`, scored 75 against the alias `OPENAI`, and was rejected -- meaning **matcher v1 was worse than the frozen patterns on this row**, which select it via `%OPENAI%`. Fixed by stripping a dot-TLD (`.COM`, `.NET`, `.ORG`, `.IO`) **before** de-dotting. **`.AI` is deliberately excluded and must stay excluded** -- `X.AI` is a company's entire identity and stripping it would undo the 85-holding recall fix that motivated de-dotting. Two rules now pull in opposite directions on the same character and only an explicit list keeps them straight; a parametrised test asserts the strip claims none of the corpus's other dot-TLD issuers (Amazon.com, Businessolver.com, Mercor.io, Way.com, Card.io, Tokens.com, Integrate.com, Xpressmyself.com).
- **Re-measured:** frozen patterns, all/macro **P 0.9916 / R 0.9792** (was 0.9873/0.9791 -- one of its three "false positives" was a true positive all along); matcher v1 **before** the fix **P 0.9958 / R 0.9958** with a five-holding false negative; matcher v1 after the fix **P 0.9959 / R 1.0000**. Hard subset: patterns **P 1.0000 / R 0.9929**, v1 **P 0.9929 / R 1.0000**. `OPENAIR.COM` resolves at **0.9231**, inside the auto-accept band. Net effect of Week 4 restated: **85 wrongly-missed holdings and 28 wrongly-included ones removed** (not 33), one wrongly-included one introduced. Threshold band unchanged: auto-accept >= 0.90 (137 of 141 hard strings), review 0.80-0.90 (4 strings), reject below.
- **Two claims corrected rather than left standing:** (1) **v1's recall of 1.0000 is not independent of this golden set** -- the dot-TLD fix was written because the set exposed the miss. Stated in `docs/entity_resolution.md` section 7 alongside the pre-fix figure, because a metric measured on the data that motivated the fix is not a validation. (2) **On the hard subset the frozen patterns now have strictly better precision than matcher v1**, since v1 claims `OPEN BAY AUTOS AI INC.` and the patterns do not. v1's case is recall, not precision; `test_matcher_v1_does_not_have_the_better_precision` pins the ordering so it cannot quietly invert.
- **Open issues:** **314 of 322 labels unattested.** The `E0` negatives are where another `OPENAIR.COM` would hide -- **78 strings, 1,310 holdings**, nothing fires on them and nothing draws attention to them; they are the obvious target for Week 5's adjudication layer. `OPENAIR.COM` needs **no** backfill of the shipped universe layer (the frozen pattern already selects it and already labels it OpenAI Group PBC, which the correction confirms), but the **28 `%COHERE%` canary holdings of Cohere Technologies are wrongly inside the shipped 5,806-row universe** and remain so -- removing them is a universe-version change, not a matcher change. `OPENAIR` clears the per-token gate at 0.9231 against a minimum of 92, a 0.31 margin and the thinnest in the system. The dot-TLD list is a list, not a principle, and a universe that later includes a `*.ai` issuer will need it revisited. All Week 4 open items from the previous entry stand: Week 3's live EDGAR path, the 206 unexplained price-consistency disagreements, the 16 unadjudicated `NPORT-P/A` amendments, no figure. **Milestone PR 1 still not opened** -- no git operations performed.

## 2026-08-28 -- Private AI Valuation Agent: Week 5 LLM adjudication measured, not adopted

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 5 -- local LLM adjudication).
- **Gate decision:** no gate cleared. The deliverable is a measurement and a recommendation, and the recommendation is to **not** adopt. `plan.md` week 5 specifies the rule applied: "Measure lift over baseline on the golden set. If there is no lift, keep the deterministic matcher and say so."
- **Inputs:** `tests/fixtures/golden_set_v1.json` v1.0.0 (322 labelled issuer strings, 7,276 holdings); `llama3.1:8b` 8.0B Q4_K_M digest `46e0c10c039e0191` via Ollama 0.33.1 on localhost, RTX 4070 Laptop 8 GB, model 5.3 GB 100% on GPU.
- **Commands:** `winget install --id Ollama.Ollama --scope user`; `ollama pull llama3.1:8b`; `python -m scripts.run_adjudication --check`; `--run` (322 calls, 1,042 s); `--score`; `python -m pytest tests/ -q` -> **136 passed**; `node scripts/conformance.mjs`.
- **Outputs:** `src/resolve/llm.py` (Ollama client + StubBackend), `src/resolve/adjudicate.py` (prompt, JSON schema, four policies, matcher v2), `scripts/run_adjudication.py`, `tests/test_adjudicate.py` (35 tests), `docs/_adjudication_results.json` (every raw reply), `docs/_adjudication_metrics.json`, `docs/entity_resolution.md` section 9; updated `requirements.txt`, `README.md`, `docs/worklog.md`, and `tests/fixtures/golden_set_v1.json` (gains `filer_families`, which the prompt needs).
- **Result: no lift; the model costs precision.** Deterministic baseline **P 0.9959 / R 1.0000 / F1 0.9979** (322 strings, macro). With the model consulted in the 0.80-0.90 review band and on unresolved names: **P 0.9449 / R 1.0000 / F1 0.9717**, a lift of **-0.0262 F1 and -0.0510 precision**. Overruling everything short of an exact match, and the model alone, are both worse still (F1 0.9696). Per holding, 1 wrongly-included holding becomes **196**. Zero call failures; schema-constrained decoding held for all 322.
- **How it fails:** of 85 strings consulted it fixed 1 and broke 14, and **every break is a promotion** -- the matcher had resolved nothing and the model named a company on a resemblance. `HYPERSCALE DATA INC` -> Scale AI on the invented ground that it "is the parent company of Scale AI, Inc."; a `SCALED AGILE, INC.` term loan -> Scale AI; `COHERE TECHNOLOGIES, INC. PREFERRED SERIES D-1` -> Cerebras; `OPEN SPACE LABS INC` -> SpaceX; and `XAI3-FT5O.AF`, one of 74 Fidelity internal security codes in the corpus, -> X.AI on three characters. **Two replies contradict themselves**: the `reason` field reads "the issuer name and title do not match any of the candidate companies" while the `company` field names Scale AI.
- **Confidence is unusable as a signal:** 1.000 on 315 of 322 answers -- only three distinct values in the whole run -- and 12 of the 15 answers that disagree with their label returned at >= 0.95 (11 of the 14 band-policy breaks). *(Corrected 2026-08-28: this first read "308 of 322" and "nine of the fourteen"; both were hand-typed and both were wrong. See the figures entry below.)* This is a direct constraint on Week 6 -- the review queue cannot be triaged by model confidence, and `test_model_confidence_cannot_be_used_to_triage` pins it.
- **Methodology finding: the Week 4 hard subset is the wrong lens for this question.** Every LLM policy scores 1.0000 precision on it, because it was defined to exclude labels decided by the name, which excludes the `E0` negatives -- and the negatives are the only place the model does damage. Reported on the full set instead, with the discrepancy explained in section 9.2 rather than left for a reader to trip over.
- **POLICY_VETO, and why it is off by default.** All 14 breaks were promotions; the 1 fix was a demotion (`OPEN BAY AUTOS AI INC.`, a used-car marketplace the matcher had claimed for OpenAI). A policy letting the model only *withdraw* a weak claim scores **1.0000 / 1.0000, lift +0.0041 precision**. Recorded with three caveats, all in section 9.5: it is consulted on **4 of 322** golden-set strings and 3 of 231 corpus pairs, so a perfect score describes four rows; it was designed after reading the failures, so this golden set motivated it and cannot validate it (same caveat as Week 4's dot-TLD fix); and those four strings are **the same four Week 4 already routes to a human review queue**, so it pre-empts a review rather than saving one. Implemented, measured, **not enabled**.
- **Throughput (plan.md: "record throughput so the cost of a full re-resolution is known"):** 3.236 s mean per call (median 3.205, slowest 3.669), 196.9 tokens/s, 205,214 tokens, 2.4 s cold load, 0 errors. Against the **shipped universe layer** (231 distinct name/title pairs, 5,806 holdings): LLM-alone 231 calls / 12.5 min; band policy 5 calls / 16 s; veto policy 3 calls / 10 s. The band policy consults 26.4% of the golden set but only 2.2% of the corpus, because the golden set over-samples near-miss negatives by construction -- quoting the golden-set rate as an operating cost would overstate it by an order of magnitude.
- **Deviation from `plan.md`:** it pins `ollama==0.4.5`; `src/resolve/llm.py` uses `requests` (already pinned) against the same HTTP API, because the needed surface is one POST to one endpoint, and it lets `OLLAMA_HOST` point at another machine. Recorded in `requirements.txt`. Ollama itself is a new **external** runtime dependency, installed per-user, no admin.
- **Open issues:** **one model, one size, one prompt.** `llama3.1:8b` at Q4_K_M only; no 70B or reasoning-tuned model was tried, and none was iterated on -- iterating a prompt against these labels would fit it to the test set, and an honest version needs a held-out split this 322-string set is too small to give up. **Share class was not scored**: the model returned a non-UNKNOWN class on 215 of 322 replies but the fixture's own class labels are parser output, so scoring one against the other measures agreement between two guesses. **The 78 `E0` negatives remain unattested** by a human and are exactly where the model failed; if some of those labels are wrong the result could move, though it would have to move a long way to close a 5.1-point gap. Determinism across Ollama or model upgrades is unverified, which is why the digest is recorded with every result. Carried forward unchanged: Week 3's live EDGAR path, the 16 `NPORT-P/A` amendments, the 28 `%COHERE%` canary holdings still wrongly inside the shipped universe layer, and **milestone PR 1 still not opened** -- no git operations performed.

## 2026-08-28 -- Private AI Valuation Agent: Week 5 figures, and two hand-typed numbers they caught

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 5 -- figures for the narration; not a plan.md deliverable, produced for the weekly video).
- **Gate decision:** none. No data or metric changed; two published numbers were corrected to match the artifacts they were always supposed to come from.
- **Inputs:** `docs/_adjudication_metrics.json`, `docs/_adjudication_results.json` (322 cached replies), `tests/fixtures/golden_set_v1.json` v1.0.0.
- **Commands:** `python scripts/make_week5_figures.py`; `npm run svg-to-png` (+ automatic `audit:layout`); `node scripts/svg-layout-audit.mjs images/private-ai-valuation-agent`; `python -m scripts.run_adjudication --score`; `python -m pytest tests/ -q` -> **137 passed**; `node scripts/conformance.mjs`.
- **Outputs:** `scripts/make_week5_figures.py`; `images/private-ai-valuation-agent/w5-{setup,scoreboard,failures,confidence,veto}.{svg,png}`; `docs/_figdata_week5.json`; a `confidence` block in `docs/_adjudication_metrics.json` and the `confidence_profile()` that computes it in `scripts/run_adjudication.py`; `tests/test_adjudicate.py` (+1 test); corrections to `docs/entity_resolution.md` §9.4, `README.md`, `docs/worklog.md`, `docs/video_script_week5.md` and the Week 5 entry above. Copied to the video folder `2026-08-28-Measuring-a-local-LLM-against-the-matcher/pantry/` with a README.
- **Result: the figures caught two wrong numbers in shipped prose, which is the point of generating them from the artifacts.** (1) Section 9.4 claimed 1.000 confidence on **308 of 322** answers and **nine of the fourteen** wrong ones at >= 0.95. Recomputed from the cache: **315** at full confidence (the model used exactly three values all run -- 1.0 x315, 0.95 x4, 0.0 x3), and **12 of the 15** answers that disagree with their label at >= 0.95, or **11 of the 14** band-policy breaks. Fifteen and fourteen are different denominators -- the model's own error set versus the damage the shipping-candidate policy would do -- and the prose had silently used the smaller one with the larger one's framing. (2) The setup figure claimed the model was offered **7** candidates; it was offered **11**. The count came from `user.count("
- ")`, which matches nothing because the candidate block is indented rather than bulleted, and fell through to a hand-typed `or 7` fallback. The four extra are watchlist companies -- and **Scale AI and X.AI, the two names the model most often promotes to, are both watchlist**, so the understated count also understated the exposure.
- **Fix, not just correction:** `confidence_profile()` now writes the counts into the metrics artifact, and `test_the_confidence_prose_is_read_from_the_artifact` recomputes them from the cache and asserts the two denominators are kept apart. The `or 7` fallback is gone; the figure reads `len(CANONICAL)`. Both defects are the same class as Week 4's -- a number that no script produced (P3) -- and both were found the same way, by generating a figure from the source data and reading it against the text.
- **Figure QA:** layout pass `0/5 flagged, 0 errors, 0 warnings`. Accuracy pass found four defects the layout pass could not: the "withheld" column's continuation line reading as a separate item; an unlabelled 1-vs-196 bar pair colliding with its own caption; the closing sentence of the failures figure running off the right edge; and the veto figure captioning the system's single false positive "correct" (it described the policy's outcome, not the matcher's claim) while two rows truncated to identical strings. All fixed and re-rendered.
- **Open issues:** Inter does not resolve in the renderer, so every non-title label falls back to monospace -- consistent with Weeks 2 and 4, but not what `brutalist/DESIGN.md` specifies. Unchanged from the Week 5 entry: one model, one size, one prompt; share class unscored; the 78 `E0` negatives unattested; determinism across Ollama upgrades unverified. Carried forward: Week 3's live EDGAR path, the 16 `NPORT-P/A` amendments, the 28 `%COHERE%` canary holdings still inside the shipped universe layer, and **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-03 -- Private AI Valuation Agent: Week 6 resolution graph and human review queue

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 6 -- resolution graph and human review queue).
- **Gate decision:** **NOT cleared -- 8 review questions are open and only a named human can answer them.** The queue is built, running and reproducible; `review_decisions` is empty by design. 1,269 of 5,806 holdings (21.9%) are blocked behind those 8 questions, 894 of them on the SpaceX suspected split. The graph rejects a decision without a named reviewer and a written rationale (P4), and rejects naming a company absent from `companies`, because admitting a company is a universe-version decision taken at the Week 1 gate.
- **Inputs:** `data/parquet/*/universe_holdings.parquet` (14 quarters, 5,806 rows) loaded to Postgres; `universe_v1.json` (frozen, Week 1); `src/resolve/match.py` matcher v1; LangGraph 0.2.60 + `langgraph-checkpoint-postgres` 2.0.9 on **local Postgres 17.2** (see blocker).
- **Commands:** `initdb`/`pg_ctl` for a local cluster on port 55432; `python -m src.db.load --all`; `python -m src.db.seed`; `python -m scripts.review_queue --run` (231 questions); `--status`; `--list`; `--report`; `--export-fixture`; `python -m pytest tests/ -q` -> **153 passed, 1 skipped**; `node scripts/conformance.mjs`.
- **Outputs:** `src/graphs/resolve_graph.py` (graph, triggers, review card, split signal), `scripts/review_queue.py` (reviewer CLI), `src/db/seed.py` (companies), `src/db/schema.sql` (+`companies`, `securities`, `review_decisions`, `match_decisions`), `tests/test_review_queue.py` (16 tests), `docs/review_queue.md` (generated), `tests/fixtures/review_decisions_v1.json` (0 decisions), `docs/entity_resolution.md` section 10, `README.md`, `DATABASE_SETUP.md`, `requirements.txt`, `docs/worklog.md`.
- **Result: 4,537 of 5,806 holdings (78.1%) resolved with no human involvement; 1,269 stopped.** By method, from `match_decisions` and not estimated: alias 2,760 holdings / 97 questions, LEI 1,681 / 68, fuzzy 57 / 8, SPV 39 / 16. The 42 paused cards collapse to **8 real questions**: SpaceX split (6 cards, 894 holdings), X.AI new_company (24 cards, 278), unresolved Cohere (2 cards, 28), Perplexity split (2, 18), SpaceX band (3, 16), Groq new_company (3, 16), Anthropic split (1, 13), Perplexity new_company (1, 6).
- **The queue independently surfaced a known contamination.** The 28 `%COHERE%` canary holdings that Week 4 logged as wrongly inside the shipped universe layer arrive as the only `unresolved` trigger. Nothing was added to make it find them: matcher v1 declines to name a company, the frozen Week 2 pattern says `Cohere Inc. [FALSE POSITIVE]`, and the review card prints both.
- **Design conflict in `plan.md`, resolved and logged (P6).** `plan.md` specifies `match_decisions ... unique (raw_id)` *and* decisions "keyed so the same ambiguity is never presented twice". Both cannot hold in one table: the 5,806 holdings are 231 distinct `(issuer_name, title_of_issue)` pairs, so a holding-keyed queue asks about Databricks 85 times. Resolution: `review_decisions` keyed by the question (case- and whitespace-normalised, stored as readable text), `match_decisions` unchanged with `unique (raw_id)`. One human answer fans out to every holding sharing the key. A second collapse applies to `new_company`, which is a question about a company rather than a string -- X.AI reaches the queue under 24 spellings, and the answer is recorded against the company so one decision retires all of them.
- **Durability tested across a real process boundary.** `test_a_paused_review_survives_a_new_process` pauses the graph in one interpreter and resumes it in a genuinely separate one, sharing nothing but Postgres: the second process finds the thread by an id derived from the question, asserts it is paused, answers it, and the decision lands in `review_decisions`. Two supporting invariants are also tested: a paused review writes **nothing** to `match_decisions`, and `decided + queued == 5,806` so no holding is ever dropped.
- **Defect found in this week's own trigger.** The split test first used a tolerance of 2% *of the ratio*, which at a ratio of 348 is a window of +/-7 -- it flagged three OpenAI questions at 348x, 320x and 306x, which are the `$1.00` placeholder rows carried since Week 2, not splits. Replaced with an absolute +/-0.02 window and a ratio capped at 20; flagged questions fell 19 -> 9 and the survivors are SpaceX and Perplexity at exactly 10.0000 and Anthropic at 4.0181. `plan.md`'s own verification case (Perplexity 695 -> 58, ratio 11.983) is inside the window. A test asserts both what the rule catches and what it must not.
- **Three further defects, fixed:** `match_decisions.decision_key` carried a foreign key to `review_decisions`, which rejected every auto-accepted holding -- the key is a grouping identity, not a reference (FK dropped, index kept). The review card became an unprintable binary blob when redirected, because Windows hands Python a cp1252 stdout that encodes the card's em dashes as invalid UTF-8; the CLI now forces UTF-8 (P5 -- the card is a human artifact and has to survive a redirect). And the first band test asserted the wrong side of the boundary: `DXYZ SpaceX I LLC` scores 0.90 exactly and is accepted, so the test now uses the Week 4 blended-class MWAM string at 0.80 and a second test pins the band ceiling as exclusive.
- **Model confidence is deliberately not a trigger, and the adjudicator is off by default.** Week 5 measured 1.000 confidence on 315 of 322 answers including 12 of the 15 wrong ones, so a queue sorted by it would put the wrong rows last. `--model` will add a suggestion to a card, labelled as a model judgment and printed beside its measured failure rate (P8).
- **Blocker, the user's:** **the project's Supabase instance is unreachable.** `DATABASE_URL` names `db.<ref>.supabase.co`, which no longer resolves -- a paused free-tier project, or the retired IPv4 direct host (the pooler host that `DATABASE_SETUP.md` section 1 prescribes does resolve from this machine). All of Week 6 therefore ran against a **local Postgres 17.2** cluster, `initdb`-ed into the session scratchpad without administrator rights, rebuilt from the local Parquet layer -- and it reproduced the documented counts exactly: 5,806 universe holdings across 14 quarters, 1 null price, 64 SPVs, `complete=true`. The workaround is written up in `DATABASE_SETUP.md`. Fixing the connection string is a credential change and it is the user's.
- **Open issues:** **the 8 review questions are unanswered** and `tests/fixtures/review_decisions_v1.json` has 0 decisions, so the regression test that turns human decisions into test cases skips rather than passes -- the mechanism is exercised in the test database with a synthetic `test-reviewer` identity, never with a real person's name. **The 42 paused threads live in the local cluster** and need re-running rather than migrating once the real database is back; the graph is deterministic, so `--run` reproduces the same 8 questions. `securities` is created and empty (`match_decisions.security_id` null until Week 7). The split trigger does not catch the `$1.00` OpenAI placeholder -- a different defect, still open. Carried forward unchanged: Week 3's live EDGAR path, the 16 `NPORT-P/A` amendments, 314 of 322 golden-set labels unattested, the 28 `%COHERE%` holdings still inside the shipped universe layer (their removal is a universe-version change, and the queue now names them), and **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-04 -- Private AI Valuation Agent: the review queue survived an unplanned crash

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 6 -- follow-up to the entry above; no new work, one correction and one piece of evidence).
- **Gate decision:** none. The 8 review questions from the Week 6 entry remain open.
- **What happened:** the local Postgres 17.2 cluster standing in for the unreachable Supabase instance was started from a long-running foreground command, and when that command was terminated the server died with it -- `server process (PID 13080) was terminated by exception 0xC0000142`, a failed child-process initialisation, which is how a torn-down session looks from inside Postgres. A report written minutes earlier had stated the cluster was running; it was not, and the statement was corrected.
- **Result: the queue came back whole, which is the durability claim tested by accident.** Restarting the same data directory replayed WAL and restored every count: **5,806 raw_holdings, 4,537 match_decisions, 231 checkpoint threads, 11 companies, 0 review_decisions, and the same 42 paused reviews behind the same 8 questions.** `test_a_paused_review_survives_a_new_process` proves a paused review survives a *process* exiting; this was a server crash and a cold restart, which is a stronger fact than the test asserts and was not planned.
- **Fix:** `DATABASE_SETUP.md` now puts the port and `listen_addresses` in `postgresql.conf` instead of passing them with `-o`, and redirects `pg_ctl`'s own output so it does not hold the launching shell's pipe open. Started that way the server outlives the shell. The failure mode and the recovery are both written up beside the instructions, because the instructions as first written produced a server that looked fine and then vanished.
- **Open issues:** unchanged from the Week 6 entry. The 8 questions still need a named human; the 42 threads still live in a local cluster and still need re-running rather than migrating once Supabase is reachable; **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-04 (later) -- Private AI Valuation Agent: Week 6 re-run on Supabase; blocker closed

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 6 -- resolution graph and human review queue).
- **Gate decision:** none, and the Week 6 gate is still **NOT cleared**: the 8 review questions remain open and only a named human can answer them. What closed is the infrastructure blocker, not the gate.
- **Blocker closed:** the project's Supabase instance is reachable. `db.<ref>.supabase.co` resolves again, **to an IPv6 address only** (`2600:1f11:...`), which identifies the cause as the retired IPv4 direct host rather than a paused project -- it fails with `Name or service not known` from a host without IPv6 egress and succeeds from one with it. Server is **PostgreSQL 17.6** on Linux; the stand-in was 17.2 on Windows.
- **Commands:** `python -m src.db.connect` (applied the Week 6 schema to Supabase); `python -m src.db.seed` (11 companies); `python -m scripts.review_queue --run`; `--status`; `--report`.
- **Outputs:** `companies`, `securities`, `review_decisions`, `match_decisions` and the four `langgraph-checkpoint-postgres` tables created in Supabase; 4,537 `match_decisions` rows and 231 checkpoint threads written there; `docs/review_queue.md` regenerated; `docs/entity_resolution.md` section 10, `README.md`, `docs/worklog.md` updated.
- **Result: byte-identical to the local run, on a different server, with no state migrated.** 231 questions, 189 auto-accepted, **4,537 of 5,806 holdings decided (78.1%)**, 42 paused, **1,269 waiting (21.9%)**. By method: alias 2,760 / 97 questions, LEI 1,681 / 68, fuzzy 57 / 8, SPV 39 / 16. The same 8 open questions in the same order: SpaceX split (894 holdings), X.AI new_company (278), Cohere unresolved (28), Perplexity split (18), SpaceX band (16), Groq new_company (16), Anthropic split (13), Perplexity new_company (6). The Week 6 entry predicted the paused threads would need "re-running, not migrating, because the graph is deterministic"; that prediction is now measured rather than claimed.
- **Retired:** the local Postgres 17.2 cluster is stopped. Its data directory stays in the session scratchpad and is disposable. `DATABASE_SETUP.md` keeps the recipe deliberately -- it needs no administrator and it is the right answer when the network or the hosted project is unavailable.
- **Open issues:** **the 8 questions are unanswered**, 1,269 holdings blocked behind them, 894 on the SpaceX suspected split; `review_decisions` is empty and `tests/fixtures/review_decisions_v1.json` has 0 rows, so the regression test that turns human decisions into test cases skips rather than passes. `securities` is created and empty (`match_decisions.security_id` null until Week 7). The split trigger still does not catch the `$1.00` OpenAI placeholder. Carried forward unchanged: Week 3's live EDGAR path, the 16 `NPORT-P/A` amendments, 314 of 322 golden-set labels unattested, the 28 `%COHERE%` holdings still inside the shipped universe layer, and **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-04 (latest) -- Private AI Valuation Agent: Week 6 gate CLEARED, review queue empty

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 6 -- resolution graph and human review queue).
- **Gate decision:** **CLEARED** by **Om Mali**, 2026-09-04. All 8 open review questions answered in seven commands against Supabase Postgres 17.6. **1,269 holdings decided by a human; 5,806 of 5,806 (100%) now carry a resolution decision.** Attestation recorded in `review_decisions` (45 rows), exported to `tests/fixtures/review_decisions_v1.json` v1.0.0, and written up as `docs/entity_resolution.md` section 10.8. Every decision names the reviewer and carries a written rationale; the graph rejects a blank of either.
- **Inputs:** the 42 paused LangGraph threads from the entry above; `docs/review_queue.md` as the reviewable surface; evidence assembled for each question from `raw_holdings` (share counts, values, per-period prices, asset categories) before any answer was given.
- **Commands:** `python -m scripts.review_queue --decide-company` x5 (SpaceX, X.AI, Groq, Perplexity, Anthropic) and `--decide` x2 (the two Cohere series); `--export-fixture`; `--report`; `--status`; `python -m pytest tests/ -q` -> **154 passed, 0 skipped**; `node scripts/conformance.mjs`.
- **Outputs:** 45 `review_decisions` rows and 1,269 `human` rows in `match_decisions` on Supabase; `tests/fixtures/review_decisions_v1.json` (45 decisions, was 0); `docs/review_queue.md` (empty queue); `docs/entity_resolution.md` section 10.8 and updated 10.1/10.7; `README.md`; `docs/worklog.md`.
- **Result: 100% of the universe layer is resolved, and the split findings do not agree with each other.** All three `split`-triggered questions (9 cards, 925 holdings) were answered "this is the company", but for three different reasons, and a Week 7 detector that treats them alike will be wrong two times in three. (1) **SpaceX, 894 holdings, NOT a split** -- a filing-unit artifact, since the same fund reports common at 81.00 and preferred at 810.00 on the same date, consistent with Week 2's 309 fund/period groups at exactly 10.000; do not adjust. (2) **Perplexity, 18 holdings, a REAL 10:1 split** -- share count 6,081 -> 60,810 with dollar value identical at $4,228,994, which is the arithmetic signature of a split and of nothing else; adjust. This is `plan.md`'s own stated verification case and it was caught before it could enter a price series as a 90% crash. (3) **Anthropic, 13 holdings, a repricing** -- 12.18 -> 48.94 fits the verified staircase (12 -> 48 -> 140 -> 259) and the 4.0181 ratio near 4 is coincidence; do not adjust.
- **The `%COHERE%` canary is formally rejected.** 28 holdings, verdict `not_in_universe`, on the written ground that Cohere Technologies is a different company and the rows were selected by the deliberate Week 4 canary pattern. The holdings remain in `raw_holdings` because it is append-only; their removal from the universe layer is a universe-version change and is still open.
- **Decisions are now regression tests.** `test_every_exported_human_decision_is_still_honoured` reads the 45 exported decisions and fails if the deterministic matcher contradicts one. It was skipping for want of decisions and now runs and passes. 45 rows cover 42 cards because each of the three `new_company` answers also wrote a company-level key, so a 25th X.AI spelling arriving next quarter resolves without asking anyone -- the "never asked twice" guarantee now has real data behind it rather than a test fixture.
- **Open issues:** nothing outstanding in Week 6. `securities` is created and empty and `match_decisions.security_id` stays null until Week 7 populates it. The split trigger still does not catch the `$1.00` OpenAI placeholder -- a different defect, still open. Carried forward unchanged: Week 3's live EDGAR path, the 16 `NPORT-P/A` amendments, 314 of 322 golden-set labels unattested, the 28 `%COHERE%` holdings still physically inside the shipped universe layer pending a version boundary, and **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-10 -- Private AI Valuation Agent: Week 7 marks panel and split detector

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 7 -- marks panel and the split detector).
- **Gate decision:** **NOT cleared -- 3 suspected splits await a named human.** The panel is built, verified and reproducible; `split_adjudicated` is false on all 11 suspected marks and nothing adjudicated itself. `scripts/build_marks.py --adjudicate` records a verdict and requires a reviewer name and a written reason (P4).
- **Inputs:** `raw_holdings` (5,806 rows, 14 quarters), `match_decisions` (5,806 rows, Week 6), `companies` (11), Supabase Postgres 17.6. World Labs is checked against the local Parquet private layer because it is not a universe member.
- **Commands:** `python -m src.db.connect` (marks, security_map, and four columns on securities); `python -m scripts.build_marks --build`; `--verify`; `--report`; `--panel`; `python -m pytest tests/ -q` -> **173 passed**; `node scripts/conformance.mjs`.
- **Outputs:** `src/marks/build.py`, `src/marks/splits.py`, `src/marks/verify.py`, `scripts/build_marks.py`, `tests/test_marks.py` (19 tests), `docs/marks_panel.md` (generated), `src/db/schema.sql` (+`marks`, +`security_map`, +4 columns on `securities`), `README.md`, `docs/worklog.md`; corrections to `src/graphs/resolve_graph.py` and `tests/test_review_queue.py`.
- **Result: 5,479 marks over 232 securities, 55 period ends and 861 company/manager/period rows.** 5,185 priced, 294 unpriced on purpose, 305 blocked from any change series, 27 opaque SPV marks reported rather than dropped. Re-mark against carry-forward, which Week 8 needs: **3,067 re-marked, 1,019 carried forward, 1,099 first observations**. **Coverage reconciles exactly**: 5,806 holdings = 28 rejected in Week 6 + 72 superseded by amendment + 5,706 aggregated into marks, asserted by a test.
- **Defect found and corrected: the split rule missed `plan.md`'s own verification case.** Week 6's rule was an absolute +/-0.02 window on the ratio, capped at 20. `plan.md`'s verification list requires that Perplexity's 695 -> 58 be flagged; that ratio is **11.93**, which an absolute 0.02 window around 12 misses by a factor of three. The rule looked correct only because the other Perplexity step lands on exactly 10.000. Replaced with a **relative 1% window under the same cap**, defined once in `src/marks/splits.py` and imported by the Week 6 queue trigger so the two cannot diverge. It still excludes X.AI's 2.064x repricing (44 occurrences), Cerebras at 2.457x and Figure AI at 8.299x, and still excludes the 348x `$1.00` placeholder artifact -- by the cap, which was always the part that mattered. `tests/test_review_queue.py::test_the_split_rule_is_capped_and_relative` was rewritten; its predecessor asserted the wrong property in its own name.
- **Split detector, two tiers, neither of which adjusts anything.** 11 marks are `split_suspected` and blocked: Perplexity PFD:E-1 and PFD:D-1 at 10.0000 (695.44 -> 69.54), Perplexity COM at 11.9300 (695.04 -> 58.26, plan.md's case), Anthropic COM at 4.0181 (12.18 -> 48.94), and SpaceX CLASS A / CLASS C / PFD:E at 2.0000 (212 -> 424). 124 steps are large moves whose ratio is not near-integer -- routed for a look and **left in the series**, because a repricing is the signal the project exists to measure. The Week 6 identity-review rationale is attached to each suspected mark as evidence and explicitly **not** as an adjudication: the reviewer was answering "which company is this".
- **Three structural facts the data forced, all deviations from the plan's schema sketch.** (1) **Amendments supersede**: all 16 `NPORT-P/A` filings restate a (fund, period end) that already had an original, so the marks uniqueness constraint would have rejected the second filing and the survivor would have been an accident of insert order; the amendment wins and 72 original holdings never become marks. (2) **A security arrives on several lines**: 189 line groups within a single filing share issuer name and title (464 rows); 151 agree on price and are summed, and the rest disagree -- SpaceX common and preferred under an identical title, ten times apart -- so the mark is recorded **blended with no price**, retaining price_min and price_max. 6 marks. (3) **`asset_category` is part of identity** (it is the only thing separating SpaceX common from preferred inside one filing) **but cannot be part of the security key**, because three Databricks titles saying "Series I" are filed EC by one manager and EP by another -- one security, two tags. Hence `security_map`.
- **A rule written too strong, then narrowed.** The detector first blocked every mark of a security whose series broke anywhere, which quarantined 90 further marks including the whole of ARK's Anthropic series -- for a break that a change between two other periods never crosses. `plan.md`'s invariant is per mark. Now `series_flagged` warns and `change_blocked` blocks only the step; a test pins the distinction.
- **plan.md's end-to-end checks: 6 pass, 1 unreachable, 0 fail.** Anthropic/Fidelity/2026-03-31 reproduces exactly from accession `0000035402-26-003406` (46,814 shares, $12,131,380, 259.1400 recomputed). Manager agreement is **stronger than the plan claims**: 10 families agree at $259.14 to the cent, in two clusters that disagree at four decimals -- 259.1364 (Alger, BlackRock, Capital Group, Coatue, Franklin Templeton, JPMorgan, New York Life, T. Rowe Price) and 259.1400 (ARK, Fidelity). A first version of that check reported 8 because it took only the largest cluster. The ~$589 pair is **structurally unreachable**: those period ends are 2026-05-29 and 2026-05-31, which sit in the unpublished 2026Q3 bulk set, and the newest period in the panel is 2026-04-30; reported as N/A with the reason, not as a pass.
- **Open issues:** **3 suspected splits await adjudication** (Perplexity, Anthropic, SpaceX), and until then 11 marks stay out of every change series. **Non-USD currency is guarded but unexercised**: all 5,806 holdings are USD, so the code path has never run against a real non-USD row and should not be described as tested. The `$1.00` OpenAI placeholder rows are excluded from pricing by the not-a-share-price basis (230 marks) rather than by a currency or split rule. Carried forward unchanged: Week 3's live EDGAR path, 314 of 322 golden-set labels unattested, the 28 `%COHERE%` holdings still physically inside the shipped universe layer pending a version boundary, and **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-11 -- Private AI Valuation Agent: Week 7 splits adjudicated; a silent-parameter defect found

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 7 -- marks panel and the split detector).
- **Gate decision:** **CLEARED** by **Om Mali**, 2026-09-11, on all three suspected-split questions covering 11 marks. Recorded in `marks.split_adjudicated` / `split_adjudication`, each carrying the reviewer's name and a written reason; the code rejects a verdict missing either. Anthropic and SpaceX return to the change series; Perplexity's 7 marks stay blocked pending the Week 8 adjustment, which is a separate gate. `series_flagged` is 0.
- **Inputs:** the 11 `split_suspected` marks from the entry above, plus the share-count and value evidence assembled per series from `marks` before any verdict was given.
- **Commands:** `python -m scripts.build_marks --adjudicate` x3 (Perplexity split factor 10, Anthropic not_a_split, Space Exploration not_a_split); `--report`; `python -m pytest tests/ -q` -> **176 passed**; `node scripts/conformance.mjs`.
- **Outputs:** 11 adjudicated marks; `src/marks/splits.py` (gains `adjudicate()` and `VERDICTS`); `scripts/build_marks.py` (now a thin wrapper); `tests/test_marks.py` (+3 tests, 22 total); `src/db/schema.sql` (+`marks.split_factor`); `docs/marks_panel.md` regenerated with verdict, factor and block state.
- **Result: the share count settled all three, and it disagreed with the ratio twice.** (1) **Perplexity is a real 10:1 split**: five T. Rowe Price funds across PFD:D-1 and PFD:E-1 show the share count multiplied by exactly ten with the dollar value unchanged to the cent -- 19,395 -> 193,950 at $13,488,132.50, 40,949 -> 409,490 at $28,477,728.17, 10,496 -> 104,960, 6,081 -> 60,810, 1,346 -> 13,460, 98 -> 980. (2) **The 11.93 step is a composite**: ARK's Perplexity common went 6,081 -> 60,810 shares while value fell $4,226,522.43 -> $3,542,758.43, so 11.93 = a 10:1 split AND a 16% markdown in one period. The adjudicated factor is therefore **10, not 11.93**; dividing by the detected ratio would erase a real price move, which is the mirror image of the mistake the detector exists to prevent, and is why `split_factor` exists as a column distinct from `split_ratio`. (3) **Anthropic is not a split**: ARK holds 89,078 shares in every one of thirteen periods, only the value moves, and the 4.02 rise to $48.94 reverses to $30.00 the next quarter -- a split never reverses. (4) **SpaceX is not a split**: Neuberger Berman's share counts are constant at 22,368 and 3,428 while value doubles exactly ($4,742,016 -> $9,484,032 and $7,267,360 -> $14,534,720), a 2x repricing that continues to $526.59 the following quarter.
- **Defect: a command-line parameter was accepted and silently dropped.** `--factor` was parsed, passed into the query parameters, and **never referenced by the SQL**, because two edits to `scripts/build_marks.py` -- the `split_factor = %(factor)s` assignment and the guard requiring a factor for a split verdict -- were lost when a patch script asserted and exited before its write. psycopg2 ignores an unused named parameter without error, so the command exited 0, printed a confirmation naming the factor, and wrote NULL. The reviewer's three commands therefore recorded correct verdicts, reviewers and rationales with a **null factor**, and one re-run is needed to fill it. **Fixed at the cause:** the logic moved from the CLI into `src/marks/splits.py::adjudicate()` where it is reachable by tests, and `test_a_split_verdict_must_carry_a_factor` plus `test_a_confirmed_split_records_its_factor_and_stays_blocked` both fail against the broken version. A guard that exists only inside a script is a guard nothing tests.
- **Also fixed:** the local Postgres test cluster's data directory was corrupted (`could not open file "base/1/4171"`), which surfaced as 13 fixture errors rather than as skips. Set aside under `pgdata.corrupt.<timestamp>` rather than deleted, and re-initialised; the marks tests then pass 22/22. The cluster is a disposable stand-in -- the project's data is in Supabase and was never involved.
- **Open issues:** **`marks.split_factor` is NULL on Perplexity's 7 marks** pending one re-run of the adjudication command; Week 8 must read that column and must not fall back to `split_ratio`. **Non-USD currency is guarded but unexercised** -- all 5,806 holdings are USD, so that path has never run against real data. Carried forward unchanged: Week 3's live EDGAR path, 314 of 322 golden-set labels unattested, the 28 `%COHERE%` holdings still physically inside the shipped universe layer pending a version boundary, and **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-11 (later) -- Private AI Valuation Agent: Week 7 closed, split factor recorded

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 7).
- **Gate decision:** no new gate. The Week 7 gate was cleared earlier today; this entry closes the defect that had left it incompletely recorded.
- **Commands:** `python -m scripts.build_marks --adjudicate --company Perplexity --verdict split --factor 10 --reviewer "Om Mali" --rationale "..."`; `--report`; `python -m pytest tests/ -q` -> **176 passed**; `node scripts/conformance.mjs`.
- **Result: `marks.split_factor` = 10.00 on all 7 Perplexity split marks**, which stay `change_blocked` pending the Week 8 adjustment. The 4 `not_a_split` marks carry no factor, which is correct, and are unblocked. The regenerated `docs/marks_panel.md` shows the week's central distinction on one row: detected ratio **11.9300**, adjudicated factor **10.0000** -- a 10:1 split and a 16% markdown in the same period, where dividing by the ratio would have erased the markdown.
- **Week 7 is complete with no open items of its own.** 5,479 marks over 232 securities and 55 period ends; coverage reconciles exactly (5,806 = 28 + 72 + 5,706); 6 of plan.md's 7 end-to-end checks pass and the 7th is structurally unreachable and says so; 176 tests pass.
- **Open issues (all carried, none originating in Week 7):** non-USD currency guarded but unexercised (all 5,806 holdings USD); Week 3's live EDGAR path; 314 of 322 golden-set labels unattested; the 28 `%COHERE%` holdings still physically inside the shipped universe layer pending a version boundary; **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-11 (latest) -- Private AI Valuation Agent: Week 7 figures and narration

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 7 -- figures for the weekly video; not a plan.md deliverable).
- **Gate decision:** none. No data or metric changed; one verification check and one figure title were corrected, and both were wrong before.
- **Inputs:** the marks panel (5,479 marks), `review_decisions`, `securities`, `companies`, `funds` on Supabase Postgres 17.6.
- **Commands:** `python scripts/make_week7_figures.py`; `npm run svg-to-png`; `node scripts/svg-layout-audit.mjs images/private-ai-valuation-agent`; `python -m scripts.build_marks --verify`; `--report`; `python -m pytest tests/ -q` -> **176 passed**; `node scripts/conformance.mjs`.
- **Outputs:** `scripts/make_week7_figures.py`; `images/private-ai-valuation-agent/w7-{panel,tolerance,evidence,factor,checks}.{svg,png}`; `docs/_figdata_week7.json`; corrections to `src/marks/verify.py` and `docs/marks_panel.md`; narration script, README and figure copies in the week 7 video folder.
- **Result: the figures caught four defects, one of them in a verification check.** (1) **`perplexity_split_blocked` failed because a human answered the question it asks about.** It required `split_suspected AND change_blocked AND NOT split_adjudicated`; once the split was adjudicated the check reported FAIL. `plan.md`'s rule is "flagged as a suspected split and blocked from the change series until adjudicated", so the check now requires flagged and blocked and reports the adjudication state instead of requiring its absence. A check that only passes before anyone reviews it is testing the wrong thing. (2) The checks figure's title **hardcoded "0 fail"** while its own table rendered one, so the title now counts failures. (3) The evidence figure selected its representative fund with `ORDER BY balance` ascending and therefore quoted a different SpaceX fund (20,841 shares) than the RUN_LOG (22,368); now deterministic and largest-first, and pointed at the class the log cites. (4) A value was rounded in a way that flattered the claim beside it -- $4,228,993.75 shown as $4,228,994 next to the words "the same dollars" -- so values now carry two decimals. Also **red was used as a warning colour**, which `brutalist/DESIGN.md` forbids; the factor figure now uses red for the answer it is about and secondary for the two wrong choices.
- **Layout pass 0/5 flagged.** The accuracy pass found what it cannot: a subtitle running off the right edge (the text element's own box stays on canvas, so the audit is blind to it), captions crowding the next section, and the palette misuse above.
- **Open issues:** unchanged from the Week 7 close -- non-USD currency guarded but unexercised (all 5,806 holdings USD); Week 3's live EDGAR path; 314 of 322 golden-set labels unattested; the 28 `%COHERE%` holdings still physically inside the shipped universe layer; and **milestone PR 1 still not opened** -- no git operations performed.

## 2026-09-17 -- Private AI Valuation Agent: Week 8 dispersion, propagation, and the Cerebras study

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 8 -- dispersion, propagation, and the Cerebras study).
- **Gate decision:** no gate. Four measurements and a findings report; nothing adjusts a price, clears a gate, or changes the panel. **Milestone PR 2 is not opened** -- git operations are the user's.
- **Inputs:** `marks` (5,479 rows), `securities`, `companies`, `funds`, `runs` on Supabase Postgres 17.6; the raw DERA zips on disk for the Cerebras fair-value probe.
- **Commands:** `python -m scripts.analyze --run`; `--findings`; `--json`; a scratchpad probe of `2026q2` and `2026q1` raw archives at every fair value level; `python -m pytest tests/ -q` -> **189 passed**; `node scripts/conformance.mjs`.
- **Outputs:** `src/signal/findings.py` (four measurements and the guards), `scripts/analyze.py`, `tests/test_findings.py` (13 tests), `docs/findings.md` (generated, 193 lines), `docs/_findings.json`, `README.md`, `docs/worklog.md`.
- **Result 1 -- marks move MORE often than the plan expected.** 4,079 consecutive observations after the guards: **1,019 unchanged (25.0%)** against `plan.md`'s stated 30-40%. The result does not rest on rounding: treating any move under 0.1% as flat gives 26.3%, and under 1% gives 29.6% -- still below the band. Variation by company is enormous and is reported rather than averaged away: Anthropic 2.2% unchanged, OpenAI 1.1%, Groq 0.0%, against X.AI 48.5% and SpaceX 37.4%.
- **Result 2 -- managers do disagree on the same date, but mostly about which round is reflected.** 130 same-date groups of at least 3 managers on one company and class kind: **median spread 10.8%**, p90 40.5%, widest 111.9%, and 35 of 130 agree within 1%. **92 of the 130 contain two or more distinct price levels**, which is the decomposition that matters: `plan.md`'s premise "they mostly don't disagree" is too strong at face value, and its deeper claim -- that the interesting phenomenon is propagation rather than disagreement -- is what the level counts support.
- **Result 3 -- propagation is fast, and fastest for the biggest events.** 37 levels with at least 3 adopters: **median 30 days from the first manager to half the adopters**, 90 days to the last. Databricks at 190.00 across 20 managers and OpenAI at 687.6869 across 12 both reach half their adopters in **0 days**, on the same period end; Anthropic's 259.14 takes 27 days to half and 89 to all. The stagger that makes this visible: 55 distinct period ends clustering on 5 days of the month across 2022-11-30 to 2026-04-30.
- **Result 4 -- the Cerebras event study cannot be run, and the reason is structural.** `plan.md` describes it as "a free, clean, private-mark-to-public-price event study with no additional data collection". Two independent blockers: (1) `src/ingest/universe.py` filters the private layer to `FAIR_VALUE_LEVEL = '3'`, so Level 1 rows never entered Postgres -- all 5,806 `raw_holdings` rows are Level 3; (2) the Level 1 observation's period end is 2026-05-29, which sits in unpublished 2026Q3, while the archive ends 2026-04-30. Probing the raw zips directly with **no** fair-value filter returns 22 Cerebras row groups in 2026Q2 and 10 in 2026Q1, **all Level 3**, so the earliest possible Level 1 period end is after the archive ends. Either blocker alone is surmountable -- one is a filter to widen, the other a quarter to wait for -- but together they mean the comparison needs Week 3's still-open live-EDGAR path. Reported instead: the measurable Level 3 run-up, $21.46 to $100.26 across 19 period ends (+367%), last step 89.02 -> 100.26. The report **declines** to compare against `plan.md`'s $236.99 figure, because that number lives in a plan document and not in any filing this project has ingested, and a finding with no provenance is not a finding (P3).
- **Four defects in my own measurements, each caught by reading the output rather than by a test.** (1) **Dispersion compared different securities**: the first run reported 31,983% and 1,098% "spreads", which were SpaceX preferred against SpaceX common (the documented 10x filing convention) and a single Coatue mark of `OPENAI GLOBAL, LLC` carrying **150,000,000 units at $1.5072** -- an LLC interest priced per dollar of commitment, not a share price, which the class parser labelled UNKNOWN and which passed the per-share basis check. Dispersion is now computed within (company, class kind) with scale outliers set aside and named in the report. That Coatue row also pins the long-carried "OpenAI's range starts at $1.00" open issue to one specific holding. (2) **The comparison window was a radius rather than a span**: +/- 45 days is a 90-day span, and Databricks genuinely moved 28.32 -> 60.00 inside one such window, so the "dispersion" was a repricing. Now a 31-day forward span, matching `plan.md`'s own near-simultaneous example (ARK 4/30, BlackRock 5/29, Capital Group 5/31). (3) **Propagation conflated staleness with slow propagation**: first-to-last produced 427- and 396-day lags on Databricks levels, which are managers still carrying a 2023 level in late 2024. The headline is now first-to-half, with first-to-last retained and labelled for what it includes. (4) **A division by zero on a panel with no prior prices**, surfaced by a test; a first run is a legitimate state.
- **Also fixed:** a bare `%` inside an SQL comment broke a parameterised query, because psycopg2 scans comments for placeholders too.
- **Deliberately not claimed:** within-level agreement is not reported as a finding anywhere, because it is bounded by the 2% clustering threshold by construction and quoting it would restate a definition rather than measure anything. Same-date dispersion still mixes genuine disagreement with update timing -- two managers sharing a period end may have convened their valuation committees weeks apart -- and the level decomposition narrows but does not remove that. The propagation lag is bounded below by the reporting stagger, so it measures observability rather than diligence.
- **Open issues:** **milestone PR 2 not opened** (git is the user's). The `$1.00` OpenAI row is now identified but still priced in the panel; removing it is a universe or parser change rather than a findings change. Carried forward unchanged: Week 3's live EDGAR path -- which is precisely what the Cerebras study needs -- non-USD currency guarded but unexercised, 314 of 322 golden-set labels unattested, the 28 `%COHERE%` holdings still physically inside the shipped universe layer, and **milestone PR 1 still not opened**.

## 2026-09-18 -- Private AI Valuation Agent: the Cerebras event-study harness

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 8 -- the Cerebras study, completed as far as the data allows).
- **Gate decision:** none. No panel number changed; a new table was added that nothing downstream reads.
- **Route chosen by the user:** wait for the SEC to publish 2026Q3, rather than fetching the Level 1 filings from EDGAR now. Every other step was built so the study runs on arrival.
- **Commands:** `python -m src.db.connect` (+`public_observations`); `python -m src.ingest.public_marks` over all 14 quarters; `python -m scripts.analyze --findings`; `--json`; `python -m pytest tests/ -q` -> **197 passed**; `node scripts/conformance.mjs`.
- **Outputs:** `src/ingest/public_marks.py`, `src/signal/event_study.py`, `tests/test_event_study.py` (8 tests), `public_observations` (10,566 rows), `src/db/schema.sql`, `scripts/analyze.py` (+section 5), `docs/findings.md`, `docs/_findings.json`, `docs/worklog.md`.
- **Result: the Week 8 conclusion survives a much harder test.** Week 8 concluded that no Level 1 observation exists, from probing Cerebras in two quarters. Scanning **all fourteen quarters at every fair value level** returned **10,566 non-Level-3 rows**, which read as a contradiction until they were sorted: **10,126 are false positives**, **186 are the right company and the wrong instrument**, and **254 are genuine equity observations of which zero are Level 1**. The conclusion is now measured archive-wide rather than inferred.
- **Unexpected finding: the Level 3 filter was also a precision filter.** Every one of the 7,175 Level 1 rows is **Coherent Corp** (ticker COHR), matched by the deliberate `%COHERE%` canary from Week 4 -- including `CFD COHERENT CORP` and `COHERENT CORP 07/02/2029`. With `FAIR_VALUE_LEVEL = '3'` removed, the frozen Week 2 name patterns are exposed to the entire public market and their precision collapses. The 186 wrong-instrument rows are the same lesson in a different key: `X.AI LLC TL 1L BANKDEBT` and `X.AI LLC/X.AI CO ISSUER 144A` are the right issuer priced per $100 of face, which `plan.md` excludes by name. **Any future use of the public lane must route through the matcher and the review queue, not the patterns.** The canary has now caught over-matching twice.
- **Design decision: the private layer was not touched.** Widening `PRIVATE_FILTER` in place would have been a one-line change and would have silently redefined the re-mark rate, the dispersion and the panel, because 5,806 rows reconcile against `raw_holdings` as a Level 3 layer. The public lane is a separate table that nothing downstream reads. The scan reads the raw zips rather than the Parquet layer, because the Parquet layer was itself built with the Level 3 filter and a Level 1 row cannot be recovered from it.
- **The harness is tested before its data exists.** `tests/test_event_study.py` injects a synthetic Level 1 observation and asserts the arithmetic the report will print: the crossing is derived (2026-04-30 -> 2026-05-29, 29 days), the discount is computed against the **earliest** Level 1 mark rather than the lowest, a Level 2 row does not count as a crossing, and the caveats travel with the result. A harness that will not be exercised for two months has to be proven now, or November's silence is ambiguous between "no crossing" and "the code is broken".
- **Three refusals, each enforced by a test:** no hardcoded IPO date (the crossing is derived); no use of `plan.md`'s $236.99 (when the filing arrives the number comes from the filing); and the benchmark is the earliest Level 1 mark, not the cheapest, because the market moves after an IPO for reasons unrelated to how the private marks were set.
- **Open issues:** the study unblocks when `2026q3_nport.zip` is published -- roughly November 2026 on the chosen route -- after which `python -m src.ingest.public_marks 2026q3` fills the report in. The three-pile classifier in `scan_quality` is approximate and a few Level 2 LLC co-issuer notes still land in the candidate pile; the conclusion does not depend on it, since no candidate is Level 1 under any classification. Carried forward unchanged: **milestone PR 2 not opened** (git is the user's), Week 3's live EDGAR path, non-USD currency guarded but unexercised, 314 of 322 golden-set labels unattested, the 28 `%COHERE%` holdings still inside the shipped universe layer, and **milestone PR 1 still not opened**.

## 2026-09-18 -- Private AI Valuation Agent: Week 8 figures and video deliverable

- **Recipe:** `data/raw/Private_AI_Valuation_Agent/plan.md` (Week 8 -- communication artifacts).
- **Gate decision:** none. No panel number changed; figure code and prose only.
- **Commands:** `python scripts/make_week8_figures.py`; `npm run svg-to-png`; `node scripts/svg-layout-audit.mjs images/private-ai-valuation-agent` -> **0/15 flagged, 0 errors, 0 warnings**; `python -m pytest tests/ -q`; `node scripts/conformance.mjs`.
- **Outputs:** `scripts/make_week8_figures.py`, `docs/_figdata_week8.json`, `images/private-ai-valuation-agent/w8-{remark,bycompany,dispersion,window,propagation}.{svg,png}`, and `narration_script.md` + `README.md` + the five PNG/SVG pairs + `figdata_week8.json` copied to the week's video folder outside the repo.
- **Result: the accuracy pass caught four hand-typed values that the layout pass could not.** Layout was clean throughout; substance was not. A subtitle said "Nine managers" where the Anthropic preferred window holds **8 managers and 11 marks**; the red/grey split in that figure was hard-coded at `price > 220` and now runs the same single-linkage clustering as `src/signal/findings.py` (`LEVEL_REL = 0.02`); the dispersion highlight was hard-coded at `spread >= 0.4` and now uses the measured p90 (0.405, so the drawing is unchanged and the *derivation* is what was wrong).
- **One claim was false and is now removed.** The propagation figure asserted "the biggest events are the fastest: half the managers report the new level on the very same period end." Tested against `docs/_figdata_week8.json`: events with 10 or more managers have a median lag-to-half of **27 days** versus **30** for the rest -- a difference of three days -- and **12 of 28** smaller events reach half at zero days against **3 of 9** large ones. The pattern is if anything the reverse of the claim. Replaced with two counts that are true of the same data: **15 of 37** levels reach half their holders on the same period end, and **the slowest takes 92 days**. This is the second week running in which a figure's *prose* was the defect while its geometry was clean -- the two-pass rule (`AGENTS.md`) is earning its keep.
- **Cerebras is excluded from the video deliverable** at the user's instruction; the harness and its blocker remain documented in `docs/findings.md` §5 and the earlier RUN_LOG entry for today.
- **Open issues:** carried forward unchanged -- **milestone PR 1 and PR 2 not opened** (git is the user's), the Cerebras study unblocked only when `2026q3_nport.zip` publishes (~November 2026), Week 3's live EDGAR path, non-USD currency guarded but unexercised, 314 of 322 golden-set labels unattested, and the 28 `%COHERE%` holdings still inside the shipped universe layer.
