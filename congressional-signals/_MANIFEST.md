# _MANIFEST — Congressional Signal Intelligence module

Navigation map for the `congressional-signals/` module. Links to canonical sources;
does not replicate their content. Governance hierarchy:
`SNICKERDOODLE.md` (repo constitution) > repo `DOMAIN.md` > this manifest.

## Tier 1 — canonical (authoritative)

| File | Role |
|------|------|
| `recipes/congressional-signal-agent.md` | Canonical recipe spec (supersedes scripts on conflict — P6) |
| `DATA_CONTRACT.md` | Data locations + validation rules for this module |
| `logs/RUN_LOG.md` | Append-only execution truth (P7) |
| `status.md` | Current lifecycle snapshot |

## Tier 2 — task-specific (executable)

| File | Layer |
|------|-------|
| `scraper.py` | INGEST — Capitol Trades → `data/raw/trades.csv` |
| `enricher.py`, `market_adjusted.py` | GIGO — price enrich + SPY alpha → `data/verified/` |
| `cluster_analyzer.py`, `signal_scorer.py` | TOOL — cluster detect + score |
| `langgraph_pipeline.py` | TOOL — 5-node multi-agent pipeline → `logs/` + `reports/` |
| `server.py` | TOOL — FastMCP server (5 tools) |
| `dashboard.html` | Human report — interactive dashboard |
| `PAPER_DRAFT.md` | Working paper |

## Tier 3 — generated / regenerable (safe to delete)

| Path | Note |
|------|------|
| `logs/run_*.json` | Per-run agent logs (regenerated by pipeline) |
| `reports/signal_report_*.md` | Per-run human reports (regenerated) |
| `data/verified/*.json` | Cluster/profile outputs (regenerated by cluster_analyzer) |
| `__pycache__/`, `*.pyc` | Build artifacts |

> Source files, data, recipes, logs, and hand-made work are permanent — archive, never delete.
